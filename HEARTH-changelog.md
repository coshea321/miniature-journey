# Hearth — Changelog

Detailed entries for the newest ~10 versions. Older entries live in `HEARTH-archive.md`.

**Do NOT read this file end to end at session start** — read only the entries for the versions you are actually touching. Architecture and working notes are in `HEARTH-notes.md`; pending work is in `HEARTH-backlog.md`.

## ✍️ How to write an entry (added 16/08/2026)

Entries here had grown to 500–2,800 words each, written by the most expensive model at the end of the longest context in the session — and the detail was already in the PR, which is permanent and searchable. New entries are capped:

- **A headline sentence** — what changed, and the date, in the existing bold style.
- **Up to ~150 words** of substance: the mechanism, the non-obvious decision, the test that pins it.
- **Every "do NOT simplify this into X" warning**, in full — these are the entries' real value and are exempt from the cap.
- **A link to the PR** for the full reasoning. Do not reproduce the PR body here.

If a change genuinely needs more (a shipped incident, a new subsystem), write the long version in its own doc and link it, as `HEARTH-freeze-analysis.md` and `HEARTH-global-search.md` already do.

## Recently completed (newest ~10 versions — older detailed entries live in `HEARTH-archive.md`)

- **v482: Train › Pull-up Plan, a 4-week, 12-session tracker (24/09/2026).** Built from Cathal's prototype handoff. Design confirmed the same day: adapt it to the hernia rules, log finished sessions to History, and back up the progress without syncing it. There's a new Programs card, and it opens in the shared session overlay. Each session collapses, and only one is open at a time (`fl4_pullup_open`). Every set is a 44px toggle. Each exercise has a "How to" guide with Cues/Details tabs, reusing `.ycue-*` and the derived-reset rule from `physioExerciseView`. Week 4 Session 3 adds the strict attempt and a result box, and a result typed later is copied onto that session's History entry. Reset asks first and leaves History alone. `fl4_pullup` is always read through `pullupNormalise`, so corrupt data reads as no progress. A restore is additive (per-set OR). `wktSetText` renders a `target` set as "8 ✓". **Do NOT add `fl4_pullup` to the personal sync payload without a merge rule.** A plain last-write-wins would let one phone's untick erase another's ticks. Pinned by `77-pullup-plan.js`.
- **v481: Hips & Lower Back re-tuned the way Full Body was (22/09/2026).** Design confirmed by Cathal the same day. The flow is now assembled from `YOGA_HLB_BASE` + `YOGA_HLB_SLOTS` (14 slots, ids `hlb_`-prefixed because they share `fl4_yoga_variants` with Full Body's `kneehug`/`savasana`); each slot keeps the **original pose verbatim** beside a model-written reviewed version with Cues/Details. Windshield Wipers is the one plain pose; `hlb_fold2`'s reviewed side is empty (skipped). The builder is generic now: `YOGA_SLOTTED[idx]` → `buildSlottedFlow(idx)`; `buildFullBodyFlow`/`buildHipsFlow` wrap it. `allSwitch:true` adds an **All reviewed / All original** row (`setYogaVariantAll`) — Hips only, as asked. Reviewed poses carry `noPhoto:true` so they never show the original's photo. The card counts the live flow. `YOGA_TRAIN_NOTES[1]` rewritten: re-tuned, **not physio-checked**. Pinned by `76-hips-retune.js`; tests 56, 57, 74 adjusted. **Do NOT drop the `hlb_` prefix** — the two flows' knee hug and savasana would become one shared switch. **Do NOT default a slot to `orig`.**
- **v480: the rest of the v478 council review's low-priority list (22/09/2026).** **(1)** The recipe resize grip ends a drag on **`pointercancel`** as well as `pointerup` — Android cancels a touch it takes over and never sends `pointerup`, which left the move listener attached. **Do NOT drop the `pointercancel` half.** **(2)** `getBagsData` no longer re-seeds a default bag that has a tombstone, and writes nothing when there is nothing to seed; the empty state shows instead of a flicker. **(3)** `watchFetchSummary` drops a reply if the entry's `watchWikiQuery` changed while the fetch was out, instead of storing the old title's plot under the new one. **(4)** `getAuthToken` counts only HTTP **400/401/403** from the token endpoint as a rejected login; a 429/5xx now reads as `network` and stays quiet — the v468 false "log back in" in its rarer form. **(5)** Settings says Lists, Recipes and Baby can't be hidden again. Docs: `HEARTH-data-models.md` gains a Baby bags section (+ one CLAUDE.md bullet), and the v470–v473 PR links are filled in. **Tests changed: `75-council-v480.js` added; nothing existing edited.** PR to follow.
- **Docs housekeeping for token cost, no version bump (22/09/2026).** Cut roughly 150 KB (~38k tokens) from the files a session reads before any code. **(1)** This file's "newest ~10" section held 78 entries (v390–v479, 173 KB); v390–v469 moved to the top of `HEARTH-archive.md`, and `tests/checks.sh` now prints an informational note above 12 entries. **(2)** 23 finished items (struck through, CLOSED, SUPERSEDED) moved out of `HEARTH-backlog.md` § Pending into `HEARTH-archive.md` § Completed backlog items; the Cloudflare Access record became `HEARTH-cloudflare-access.md`, since it carries an incident and decisions still in force. **(3)** `CLAUDE.md` 25 KB → 14 KB: the five data-model sections moved verbatim to `HEARTH-data-models.md`, replaced by a summary naming every trigger. **Do NOT re-inline the data models into `CLAUDE.md`** — it is loaded into every session, including the ones that never touch a store. No app code changed.
- **v479: four fixes from the v478 council review, as Cathal answered them (22/09/2026).** **(1)** `YOGA_TRAIN_NOTES[1]` carries the Hips & Lower Back caution note again — written in v475, reverted with its cues in `1412675`, empty since. Now says a re-tune like Full Body's is planned. **The cues are NOT back**: Cathal chose a full Full-Body-style re-tune, which is a separate design (backlog § TRAIN CONTENT). **(2)** The failed medicine save now says it is **Hearth's own storage**, says *write it down now*, and names plant photos as the way to make room — the old "free up space" sent him to his phone's storage, which never touches the browser quota. 15px, above the 14px dose working. Mechanism unchanged. **(3)** Bag names and ids escaped everywhere the Bags view writes them into HTML, including the Unpack-all confirm (`confirmDialog` is innerHTML). **(4)** Knee Basic shows **"Each leg"** on the six each-leg moves, via a new `eachLeg:true` data flag read only by `physioEachLeg` — **Basic only, as confirmed; Full is unchanged.** **Do NOT fold `eachLeg` into `perLeg`**: perLeg drives the Right/Left set alternation, and these moves never had it. **Tests changed: `74-council-v479.js` added; nothing existing edited.** PR to follow.
- **v478: Baby › Bags stops losing bags and items to sync (20/09/2026).** Three symptoms Cathal reported together — a new bag that could not be added to, that bag showing ANOTHER bag's items, and bags/items vanishing then reappearing — were two defects. **(1)** `bags` was the last collection in `mergeBabyData` still merged as `remote.bags || local.bags`: a wholesale last-writer-wins, the exact class the v329 rewrite removed from medicine. Replaced with `mergeBags()` — bags union by id, their items union by id inside them, newest-wins by `updated`, with `fl4_tomb_bags` / `fl4_tomb_bagitems` so a delete still sticks. Every write now stamps the item AND its bag, so an active bag outranks a stale delete. **(2) Firebase stores no empty container**, so a new bag pushed with `items:[]` came back off the echo with no items key and `bag.items.filter` threw mid-render — tab row written, item list not, hence another bag's contents on screen. `normaliseBags()` now fronts every read. **Do NOT read `bd.bags` directly, and do NOT collapse the bag merge back into an array assignment.** Also: bags joined the household push's non-empty guard (a household with only bags never pushed them at all), and a backup restore merges bags instead of skipping them whenever any exist locally — which, since opening the tab seeds defaults, was always. **Tests changed: `73-baby-bags.js` added; nothing existing was edited.** PR: https://github.com/coshea321/miniature-journey/pull/244
- **v477: the Knee Programme picks a level — Basic or Full (19/09/2026).** A two-button picker on the intro, above the exercise list, shown only for a programme carrying the new **`levels:true`** flag (the Knee Programme is the only one; the other five are untouched and always full). **Basic is the default**, and `physioLevelPref()` reads anything that is not the exact string `"full"` as basic — a corrupt or half-written value **fails safe to the shorter, gentler session**, the same direction an unset yoga slot falls back to the reviewed pose. Remembered in **`fl4_physio_level`**, device-local like `fl4_yoga_variants`: not synced, not in `buildExportPayload`, so not in the backup file or the test seed. Basic changes **only** `sets` (→ 1) and `perLeg` (→ off), through `physioSetsOf(ex)` / `physioPerLegOf(ex)`, which now front every one of the five places that used to read those fields (`renderPhysioSession`, `physioNext`, `physioPrev`, the Details meta line, `finishPhysio`'s duration). **Do NOT implement this by building a second exercise array** — two lists of one programme is how a cue gets fixed in one and not the other; and it never drops an exercise, so the card's “12 exercises” stays true at either level. **A per-leg stretch at Basic runs once and the side label becomes “Do both sides”** — that line is the whole reason Basic is safe to offer, since the four side-labelled rounds used to carry the instruction by themselves; losing it silently halves the stretch. `SS.pLevel` is read **once** in `openPhysioSession`, the `SS.yFlow` rule: a session runs at the level it was begun at. No data model, sync or export change. **Tests changed: `72-knee-levels.js` added; nothing existing was edited.** PR: https://github.com/coshea321/miniature-journey/pull/243
- **v476: the Knee Programme gets Cues/Details, and the session overlay stops needing two taps (19/09/2026).** **Design:** all 12 `knee_general` exercises gained a `cues` array, and `renderPhysioSession` now renders through **`physioExerciseBodyHtml(ex, col)`** — the physio twin of `yogaPoseBodyHtml`, reusing the same `.ycue-*` classes so the two session screens stay one design rather than two that drift. The `.ycue-tab.on` and bullet colours became `var(--ycue-accent,#8b5cf6)`, set per programme on a wrapper div, so yoga keeps purple and the knee screen is green. Its meta line is the one real difference: yoga is always a hold, here it is `N reps · M sets` or `Hold for N seconds` by `ex.type`. **The reset to Cues is DERIVED, not assigned** — `physioExerciseView()` returns Details only while `SS.pViewFor === SS.pExIdx`. **Do NOT “simplify” that into `SS.pView = "cues"` at each advance**: `beginPhysio`, `physioNext`, `physioPrev` and the timer's auto-advance all move the index, and a fifth path added later would silently inherit the previous exercise's Details view. A **set** change keeps the choice on purpose — it is still the same movement. An exercise with no `cues` array (the other five programmes) renders the paragraph with no tabs, unchanged. **Bug:** Begin/Back needing two taps and the nav strip flashing on any tap were **one defect** — v467's `keyboardSheetOwner` matched any bottom-anchored fixed element, so `#sessionOverlay` took `max-height:85vh!important` and shrank off the bottom of the screen mid-tap. A sheet must now be bottom-anchored **and not full-screen**, and the `focusin` listener fires only for a control that raises the keyboard (`raisesSoftKeyboard`). **Do NOT widen either back to “any fixed element” or “any focusin”.** Carries v475 (PR #241, never merged on its own). **PR #241's Hips & Lower Back cues and note were reverted (`1412675`), not shipped** — the note returned alone in v479. No data model, sync or export change. **Tests changed: `70-keyboard-sheet-scope.js` and `71-physio-cues-details.js` both added; nothing existing was edited.** PR to follow.
- **v475: a general-purpose Knee Programme joins Train (19/09/2026).** A sixth entry in `PROGRAMMES_TRAIN` (`knee_general`, 12 exercises) plus its card in `renderTrainPrograms` — the two hand-listed halves that adding a programme always needs, and the card's `key` is the array index, so **append only, never reorder `PROGRAMMES_TRAIN`**. First programme to use `type:'reps'` (the warm-up and the six strength moves; the physio session renderer already supported it, nothing had exercised it) and the first to use `perLeg:true` — the three stretches are `sets:4, perLeg:true`, which the existing set-label code reads as two 30s holds a side, i.e. "hold 30 sec, twice each". Tagged **General fitness**, not Physio: it is a generic programme, and its `warning` says to see a physio or GP first. New optional programme field **`info`** — a neutral grey box under the intro's exercise list, `esc()`'d, carrying the after-2–3-weeks progression list. **Do NOT fold `info` into `warning`**: the amber box is for things that make you stop, and burying progression advice in it trains you to skim the box that matters. No data model, sync or export change. Tests changed: none. PR: https://github.com/coshea321/miniature-journey/pull/241
- **v474: Prep/Method/Notes collapse, and Ingredients/Prep/Method/Notes get a real drag-to-resize handle (15/09/2026).** Cathal's original ask ("expandable sections") turned out to mean resizing the text box, not folding it away — clarified after the first cut shipped collapse-only. Both are now in: Prep/Method/Notes are wrapped in a native `<details open>`/`<summary>` (Category, Ingredients, Recipe URL stay always-visible), styled to match `fieldLabel` via `recipeSectionSummary(t)`. Separately, all four recipe-editor textareas gain a **`recipeResizeGrip(id)`** drag handle — CSS `resize:vertical` alone leaves no visible grip on most mobile browsers, so this is a JS handle wired via Pointer Events (`wireRecipeResizeGrips`) that sets the textarea's height directly on drag. **Do NOT rely on `resize:vertical` alone for a mobile-facing textarea again** — it silently does nothing there. No data model, sync or export change. Tests changed: none. PR: https://github.com/coshea321/miniature-journey/pull/240
- **v473: Settings sheet regroups around Visible sections, custom categories fold behind a tap (13/09/2026).** Third pick from the 12/09 UI/UX review (backlog entry ③). The sheet was already titled "⚙️ Settings" with a proper close `aria-label` — the actual defect was the subtitle "Manage your custom categories" sitting above an empty `customCatsList`, burying **Visible sections** (the thing you actually came for) below it. Dropped the subtitle; **Visible sections** now leads, its old three-sentence description replaced with one line, "Five on the bar, the rest under More."; custom categories moved into a native `<details>`/`<summary>` disclosure (the same fold pattern already used for the Firebase setup help), collapsed by default; Import/Export and Delete all are unchanged and in the same order. `renderCustomCatsList()` still targets `#customCatsList` unchanged — it now just renders inside a closed `<details>`. No Settings tabs added. `secVisible`/`syncPrefs` restore-from-backup untouched (that's audit F4). No data model, sync or export change. Tests changed: none. PR: https://github.com/coshea321/miniature-journey/pull/239
- **v472: Recipes' four maintenance pills fold into one 🧰 Tools sheet (13/09/2026).** Second pick from the 12/09 UI/UX review (backlog entry ②). `renderRecipes`' `selectHdr` now shows just **Week plan** (kept standalone — it's a view, not a tool) and a **🧰 Tools** button opening a `.overlay`/`.modal-box` sheet (the same pattern as Weather/More) listing Tidy, Prep helper, Calories and Select as `.more-row` rows. Category chips are untouched, below the header. **Every existing click handler (`rcpTidyBtn`/`rcpPrepBtn`/`rcpCalBtn`/`rcpSelStart`) is byte-for-byte unchanged** — only the markup summoning them moved into the sheet; the sheet itself is torn down and rebuilt with the rest of `recipesContent` on every render, so no explicit close-on-navigate logic was needed. Also fixes `recipeFocusHint`: it used to show the classic-tick sentence right beside a Focus button whose on-state does the opposite. It's now hidden entirely while Focus is off and shows only the existing on-state sentence when on — no new copy. No data model, sync or export change. Tests changed: none. PR: https://github.com/coshea321/miniature-journey/pull/238
- *(v318–v469 + the 12/07 docs-housekeeping entry + the two council write-ups + the two 19/07 Fable no-bump entries (audit, design day) + the 27/07 app-privacy research note moved to `HEARTH-archive.md`, latest move 22/09/2026 (docs housekeeping moved v390–v469 in one go — the rotation had lapsed and this section had grown to 78 entries) — keep-newest-~10 rule. `tests/checks.sh` now prints a note when this section holds more than 12 entries.)*

## Shipped (reverse order, condensed — full detail in the version log above for the newest ~10, in `HEARTH-archive.md` for older)
v411 fix: personal sync could go silently receive-only (a live SSE `put` now opens the push guard, and an empty remote node is no longer mistaken for an auth failure) · v395 fix: parseIngredients strips invisible zero-width chars (AI-chat-pasted recipe had every line prefixed with U+200B, blocking all amount recognition) · v394 recipe ingredients bulleted + method auto-numbered (display-only; `methodSteps` two-mode splitter, narrow step-marker regex so temperatures/ranges/decimals survive) · v392 Watchlist "Watching" group + optional TV-only season (three states stored as two booleans for v391 mixed-version safety, cycling tick control ○▶✓ plus an explicit status picker, season clamped 1–99, episodes deliberately not built) · v391 Watchlist section (🍿 TV shows + films to watch, household-shared `fl4_watchlist`, whole-record newest-wins like Plants; title-derived JustWatch/Wikipedia/IMDb info links with no API key or stored URL, optional pasted link, TV/Film chips, collapsed Watched group, ★ rating) · v351 dead-code cleanup (duplicated 24-function workout-session region deleted after byte-identity verification, legacy sync-state duplicate vars removed, CI duplicate allowlist emptied) · v350 automated test suite slice 1 (zero-dependency CDP harness, 8 case files, CI wiring + dosing literal pins, mergeHist nameless-entry guard) · v349 chart redesign (one shared visual language for growth/weight/BP trend charts) · v348 grocery autocomplete/suggestion hist-items divergence fix (showAC items fallback + capHistToLiveLimit) · v347 baby growth chart — true WHO weight-for-length centiles via the LMS method, replacing the v310 ratio chart (5-tier centile bands, data cross-checked and Cathal-confirmed after a ~3.5% disagreement with the old approximate table) · v344 add "panadol" to paracetamol advisory match list (confirmed household brand name, advisory-only) · v343 notes sync hardening + work notes sync join (`mergeNotes` id-keyed union, both channels) · v342 fix suggestion chips reappearing after tick-off (done items' `updated` resets the clock + revives feed hist) · v341 weight-diff badge colours inverted (loss = green, gain = red — deliberate, commented) · v340 chart y-axis min/max label overlap fix (value labels to the left margin in all three SVG chart builders) · v339 Track section (nav Option B′ stage 1: Log/Food/Body re-hosted out of Notes/Train into one hideable default-on section + dead duplicate Train block removed, closing council item ④) · v338 Family Log read-only feed section (hideable + default hidden, Baby/Train/Trips/Notes events, filter chips, 90d window + load-earlier) · v337 notes toolbar "?" legend (popover listing every format button's own title attribute, back-button wired) · v336 Body weight/BP charts + Body-first Train sub-tab · v335 remove orphaned pre-v318 per-list notes subsystem (dead `tabNotes`/`notesPanel`/`renderNotesIn`/`openListNoteEditor` + CSS, unreachable since v318) · v333 fix per-person-checks toggle-on revert (Firebase strips empty `checks:{}` off the echo → explicit `{c:false,p:false,b:false}` shape) · v332 confirm dialogs on Clear-completed / Reopen-all · v331 widen `.ppc` per-person circles (council item ③, ~40×48px effective target) · v329 Baby sync hardening (shared per-entry merge on both channels + medicine updated-stamps + med deletion tombstones, council item ②) · v328 note-editor DOMPurify fallback → esc() (security, council item ①) · v327 weather rework (wind + compact layout on card · popup = hourly +wind + 3 upcoming days) · v326 per-person travel packing checks · v325 "Recent" 48h filter chip on all lists (+ per-item `added` stamp, revive paths now stamp `updated`) · v324 pre-merge test links on every PR + in-app test mode (off-domain = banner, Firebase/login hard-blocked, no SW) · v323 history per-item + multi-select delete (synced name-keyed tombstones) + grocery Clear-completed removed · v322 home live clock + back-button nav · v321 fix trip deep-link id type · v320 Today-card trip deep-link · v319 Sports Today-card entry point · v318 nav consolidation Option A (bar 7→5: Notes into Lists · Train off-bar by default · Trips packing link) · v317 Week plan dinner planner (7-day tap-to-assign + add-week-to-groceries, household-synced) · v316 weather card nearby-town name (offline Irish town table) · v315 Recipes opens on Favourites · v314 header badge leak fix · v313 weather card H/L–Rain% swap · v312 cold-start boot splash + version gate · v311 Sports recursion crash fix · v310 growth chart redesign (single WHO-ratio chart) · v309 visual growth chart · v308 sports scoreboard section · v307 suggestion-chip tap targets · v306 predictive grocery suggestions UI · v305 rain % on weather card · v304 voice capture (speak-a-list) · v303 replenishment data foundation (invisible) · v302 Today summary card · v301 growth-trend summary · v300 autofill/password-prompt fix · v299 sync-health panel · v298 trips departure badge · v297 train dedupe · v296 field-wise baby/trip merges · v295 SW lie-fi fix + tombstones 90d · v294 cruft removal (baby list + test lists) · v293 flight seats field · v292 trips boarding/gate + one-step connections · v291 dose-interval advisory + weight-date · v290 restore-dialog z-order + boot watchdog · v289 housekeeping (DOMPurify vendored · tombstones 30d · recipe stamps · touch targets) · v288 cloud restore + import gaps (trips/notes/tombstones) · v287 daily cloud backup (keep-7) + nudge 30d · v286 offline app-shell fix (network-first + cache fallback) · v285 trips scroll restore · v284 trip expand/collapse all · v283 trip 24hr times + maps link · v282 trip timeline polish · v281 trip import merge into existing · v280 trip JSON export + import · v279 fix timeline same-day ghost cards · v278 timeline view + date separators · v277 connecting flight legs + transfer time · v276 fix booking editor on sync · v275 Trips section · v274 location-based weather · v273 hourly weather strip · v272 manual grocery amount editing · v271 fix dashboard duplicate add · v270 scroll-to-top everywhere · v269 recipe search + scroll restore · v268 recipe deletion sync · v267 feels-like on weather card · v266 version label date-only · v265 timestamp diagnostic bump · v264 weight-based medicine dosing · v263 baby medicine quick-chips + babySex-save fix · v262 weather 3-day sheet · v261 home layout cleanup · v260 front-page ★ Today · v259 weather timestamp · v258 weather card · v257 tap-zone fix · v256 "Today" picks · v255 partner-recipe toast · v254 noindex · v251–253 backup nudge · v250 method formatting · v249 bulk categorise · v248 recipe-editor-no-close · v247 list convergence re-push · v246 grace window removed · v245 union/newest-wins/tombstones · v243 sync send-wedge fix · v242 newest-wins recipe sync · v241 grace catch-up · v240 recipe sync · v237–239 add-to-grocery (names→amounts→headers) · v236 recipe categories+favourites · v234 CSV import · v232 Recipes section.

