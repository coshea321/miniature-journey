# Hearth — Notes & State of Play (as of v379) 

**This is the single source of truth for the Hearth project.** It combines: state-of-play (version log + what's pending), architecture & working notes, and hard-won lessons. The only other context doc is `HEARTH-archive.md` (detailed changelog entries older than the newest ~10 versions, split out 05/07/2026) — **read it only when an old version is referenced**, not at session start.

**This file lives in the repo** (`HEARTH-notes.md` at root) and is updated by Claude Code at every release. A new chat session can read it directly — no upload needed. To get a fresh session up to speed: open Claude Code on the web, point it at this repo, and it reads `CLAUDE.md` + `HEARTH-notes.md` automatically.

## ⚙️ Maintenance habit (keep this note)
**Update this file at every release** — changelog entry (keep "Recently completed" to the newest ~10 detailed entries, moving the surplus oldest to `HEARTH-archive.md`), Current version line, and backlog sync (workflow step 5).

## Current version
**v394 · 03/08/2026** — last shipped build (version label is **date-only**, sourced from `sw.js` VERSION constant — but since v382 **only when the announced SW version is not NEWER than the loaded page**; see the v382 entry).

## External review triage (02/07/2026 — Cathal supplied 3 independent app reviews; full plan agreed)
Claims verified against code before acting. **False/already-handled (do NOT re-chase):** "apostrophe DOM crasher" — `esc()` already escapes `'`→`&#39;`; "SW cache grows forever" — old caches are deleted on every activation (cache name = version); "version maintained in multiple places" — label is already runtime-sourced from sw.js, the dual bump is deliberate; "localStorage quota unhandled" — `storeSet` already catches QuotaExceededError with a toast; "38 duplicated Train functions" — stale count from an old incident, **7 real duplicates remain** (trainTodayStr, trainShiftDay, trainFmtDate, trainFmtDateLong, timerCircle, wireInputs, wireWorkoutLog — second definition silently wins; dedupe = v297). **Real and accepted:** SW "lie-fi" hang (fixed v295); Baby object rebuilt from a hand-typed field list in sync-apply → future fields silently wiped (babySex incident class; fix = v296 field-wise merge, same for bookings); tombstone window (90d in v295). **Rejected on philosophy/churn (all three reviews pushed these):** splitting the file into modules, IndexedDB migration, incremental renderers, execCommand replacement, desktop cosmetics — single-file + guardrail workflow stands; CSS extraction is the safe first slice IF maintainability pain ever becomes real. **Tier 2 queue (agreed, each needs a design confirm):** Trips "next departure in X days" badge · "Today" aggregator card on Home · ~~Baby percentile trend~~ — **shipped v347** (21/07/2026; the "WHO data already present" premise was wrong, the app had medians only — the shipped version adds real LMS tables) · sync-health panel in Settings. Parked: dashboard customisation, ~~recurring items~~ — **shipped v361** (27/07/2026), ~~recipe-parser second pass~~ — **shipped v365** (28/07/2026), ~~global search~~ — **design confirmed 02/08/2026**, see the dedicated Pending entry below and [`HEARTH-global-search.md`](HEARTH-global-search.md), polish bundle (empty-state copy, small animations).

## Recently completed (newest ~10 versions — older detailed entries live in `HEARTH-archive.md`)
- **v394:** **Recipe ingredients render as bullets, the method as a numbered list (Cathal's ask 03/08/2026: "can we auto format the recipe ingredients to use bullet points and the directions to use numbered points?"; small and well-defined — would have been fine on plain Sonnet).** **Display-only, in `renderRecipeDetail` — the stored recipe text is never rewritten**, so opening the editor still shows exactly what was typed or pasted, and nothing syncs as a result of looking at a recipe. **Ingredients:** the `<ul>` already existed but every `<li>` carried `list-style:none`, so there were no bullets — now `list-style:disc` with the list indented 20px; the `<li>` takes the accent colour (#B5651D) so the bullet is warm and an inner `<span>` puts the text back to the reading colour. Section headers (`isHeaderIng`) keep `list-style:none` and get `margin-left:-20px` to stay flush left, so "For the sauce" doesn't sit in the bullet column. **Method:** `formatMethod` used to emit `<p>`/`<br>` paragraphs — it now emits a real `<ol>` and the numbers come from the list, not the text, so a typed "1." can never double up as "1. 1.". Split out into a new **`methodSteps(text)`** helper returning `{type:"step"|"head", text}`, with **two splitting modes** because a pasted method is either explicitly numbered or it isn't: with 2+ marked lines a marked line starts a step and every unmarked line after it is a **continuation** (a hard-wrapped paste stays one step instead of exploding into fakes); unnumbered, one line = one step unless the line reads as a wrap (starts lower-case AND the previous step didn't end a sentence). **`METHOD_STEP_MARK` is deliberately narrow** — 1–2 digits and a `.`/`)`/`:` that must be followed by a space — so "180C. Bake…", "2 - 3 minutes", "1.5 kg" and "1/2 cup" are never mistaken for markers and stripped (pinned by a test). Bullet glyphs in a pasted method are dropped. A short line ending in ":" ("For the sauce:") renders as a sub-heading, not a step, and the `<ol>` **reopens with `start=N+1`** after it so numbering continues instead of restarting at 1. A single unbroken block of prose stays a plain paragraph — a lone "1." in front of it is just noise. The v250 run-together split ("…cool. 2. Whisk…") is preserved. Escaping via `esc()` is unchanged. **Tests: `tests/cases/31-recipe-step-list.js` ADDED (23 assertions) — a new file, no existing test modified.** Covers both splitting modes, the continuation rules, marker stripping, the numbers-not-eaten cases, sub-headings and `start=` continuation, escaping, the single-paragraph case, and an end-to-end detail-view render checking computed `list-style-type` on ingredient vs header rows plus the stored method text being untouched. **34/34 files green.** Verifier: SAFE TO COMMIT.
- **v393:** **Recipe URL — a place to save a link to the original recipe (Cathal's ask 03/08/2026: "add a place to add urls for each recipe"; build-ready for plain Sonnet, no design call needed).** New optional `url` field on recipe records. **Editor:** a "Recipe URL (optional)" text input added after Notes in `openRecipeEditor`, saved on both the new-recipe and existing-recipe paths. **Detail view:** a "Source" block renders after Notes when `r.url` is set — a clickable 🔗 link, `target='_blank' rel='noopener noreferrer'`. **New helper `normalizeRecipeUrl(u)`:** trims, and prepends `https://` when the typed value doesn't already start with `http://`/`https://` — this is a safety guard as much as a convenience: without it, a value like `javascript:alert(1)` typed into the field would land in an `<a href>` unescaped (`esc()` only escapes HTML metacharacters, not the URL scheme) and execute on tap; forcing the string through the `https://` prefix neutralises that class of input while also letting Cathal type a bare `example.com/recipe` without the scheme. No data-model change beyond the one new field — recipes have no dedicated JSON import/export (only the CSV importer, unchanged; it already folds a "source" column into notes text, which was left as-is since a dedicated field for manually-added recipes doesn't affect that separate CSV path), so no other sync/export site needed updating. `getRecipeBook()`'s existing null-guard (v385) and the whole-record newest-wins recipebook sync contract already carry a new scalar field through untouched (same class as the `inRotation` field added earlier — no special-casing needed). **Tests:** none added — this is a pure UI/data-field addition with no merge, dosing, or parsing logic of its own; `node tests/run.js` re-run as-is, **33/33 files green** (no regression). Verifier: SAFE TO COMMIT.
- **v392:** **Watchlist: a "Watching" group and an optional season (Cathal's ask 03/08/2026, minutes after v391's PR opened: "let's add a watching section as well and add an optional ability to add what season we're on, episode not needed").** Shipped as a **second commit on v391's branch/PR #153, deliberately breaking the one-PR-per-version rule** — v391 was still unmerged, and v392 edits v391's own code, which exists nowhere else. A fresh branch off `main` would not have contained the code being changed. PR #153 was retitled and its body rewritten to cover both versions; the merge-state hazard the rule guards against was handled by re-checking the PR was still open immediately before pushing. **"Watching section" read as a third group on the Watch screen, not a new nav section** — stated in the reply rather than asked, since a whole nav entry for "things in progress" would be an odd shape and the surrounding words ("as well", alongside the season field) point at the list. **The one genuinely load-bearing decision: three states are stored as TWO BOOLEANS (`watching` + the existing `watched`), NOT one `status` string.** v391 is already in Cathal's hands via the test link and may reach a phone before v392 does, so the store must stay readable by a v391 build: it knows `watched`, would read `status:"watching"` as *not watched* for the entire list, and would push that back. An unknown `watching` field it simply ignores, and `mergeWatchData`'s v296 field-fill carries it through untouched. `watched` always wins and `watching` only means anything while `watched` is false — an invariant `watchSetStatus` enforces on every write, pinned by a test. `watchStatusOf(w)` is the single interpreter; nothing else reads the raw booleans. **`watchToggleWatched` was deleted, not left alongside the new functions** (the codebase's duplicate-definition history makes a superseded twin a real hazard) — replaced by `watchSetStatus(id, status)`, `watchCycleStatus(id)` and the `WATCH_CYCLE` table. **The tick control now cycles in the order things actually happen** — not started ○ → watching ▶ → finished ✓ → not started — and the expanded row *also* gets an explicit three-button status picker, so the cycle is a shortcut for people who know it rather than the only route. Groups render **Watching first** (most-recently-touched first), then To watch, then the collapsed Watched group; "what are we in the middle of" is the question this screen gets asked most. **Season is optional, TV-only on screen, and clamped 1–99** (`watchSeasonOf`, 0 = unset, junk reads 0 not NaN) — episodes deliberately not built, exactly as asked. The editor's season row is **hidden rather than removed** when the kind is Film, and a season left behind by switching a show to a film **stays in the record while not rendering** — never destroy something he typed; both halves are pinned by tests. Leaving `watched` still preserves the ★ rating (the v391 rule, now applying to a three-way move rather than a toggle). No sync-protocol, export-key or store changes — `watching` and `season` ride inside the existing `fl4_watchlist` records, so all four sync paths and the backup needed no edit at all. **Tests: `tests/cases/30-watchlist.js` MODIFIED, 49 → 68 assertions — a deliberate test change, declared in the PR body per policy, and necessary because `watchToggleWatched` (which three assertions drove) no longer exists.** New coverage: a v391-shaped record with no `watching` field still reading correctly (the mixed-version case), the watched-wins invariant both ways, the full three-step cycle including `watchedAt` being stamped on arrival at watched and cleared on leaving, an unrecognised status being refused rather than corrupting the record, the -1 guard extended to the two new mutators, `watchSeasonOf` clamping 0/-1/100/"abc"/null to 0, the Watching group rendering *above* To watch, the season chip showing on a TV entry, a film's stale season being invisible **but still stored**, and the expanded row offering all three statuses. Verified non-vacuous twice: making `WATCH_CYCLE` skip "watching" fails the cycle assertions with the entry jumping straight to watched; removing the `meta.key === "tv"` gate fails exactly one assertion, with "Season 5" appearing on the film. Restored, re-confirmed **33/33 files green**. Verifier: SAFE TO COMMIT.
- **v391:** **Watchlist — a new 🍿 section for TV shows and films to watch, household-shared (Cathal's ask 03/08/2026: "a place to record a list of tv shows and movies to watch, also optionally link to free source of info for each"; designed and built on Opus, correctly — he opened with "this is not fully thought out", which is exactly the fuzzy-design trigger in the standing model-check rule).** **Three design calls confirmed via AskUserQuestion before any code was written:** ① **its own hideable section**, not a 5th list type — the four list types are hard-coded in **13 separate sync/backup/export sites** (scout-verified) and a watchlist carries fields (`kind`, `rating`, `watchedAt`) none of them have, so the list route would have meant editing 13 working sync paths for a fun feature; the section route is more new code but it is *all* new code, touching nothing that already works. ② **Auto info-links plus an optional pasted link** (he picked both over either alone). ③ **Fields = title + TV/Film + watched tick + free-text note + ★ rating**; "who wants to watch it" was offered and **not** picked — deliberately left out, logged in Pending below rather than silently built. **Data model:** `fl4_watchlist`, household-shared, whole-record newest-wins by `updated` with `fl4_tomb_watchlist` tombstones — a straight copy of the Plants (v377) contract, because one title is one edit unit and needs no nested merge. Records are `{id, title, kind:"film"|"tv", note, link, watched, watchedAt, rating, addedBy, added, updated}`. `mergeWatchData` carries the v296 field-fill (an older build's copy can't wipe a field it never heard of) and drops nulls on **both** sides — `mergePlantsData` returns nulls untouched, a latent gap this copy deliberately closes rather than inheriting. **The info links are the interesting part and are deliberately not an integration:** three chips per entry — JustWatch (where it's streaming, `/ie/`), Wikipedia, IMDb — **built from the title at render time via `encodeURIComponent`, never stored and never fetched.** No API key, no third-party code in the page, nothing that can go stale, and zero network traffic until he actually taps one. They're *search* URLs, not deep links, precisely because resolving a title to a real id would need exactly the API dependency this avoids (the same reasoning that rejected reverse-geocoding in v274 and nutrition APIs in v317). The optional pasted `link` reuses the list item sheet's own normalisation verbatim — bare domains get `https://`, anything that isn't http(s)/mailto/tel is refused with a toast. **UI:** rows with a tick circle (tap = watched) and a tappable body that expands in place to show the info chips, any pasted link, the ★ rating (watched entries only) and Edit/Remove; watched entries drop into a **collapsed "Watched · N" group**, the Done-group idiom he already knows from Lists. Kind chips (All / 🎬 Films / 📺 TV) appear only when both kinds are actually present, and a filter whose kind vanishes (retagged, removed, or dropped by a sync from the other phone) falls back to All rather than showing an unexplained empty list — the v379 pattern and the v383 empty-state lesson applied on the first try. Un-ticking **keeps** an existing rating rather than clearing it: a mis-tap must not silently destroy something he typed. **Nav default is ON, deliberately and against the recent precedent** (sports/train/famlog/plants all default off) — this is a feature he asked for, and a section he'd have to go hunting in Settings to switch on isn't delivered; the bar goes to 7 icons, which is already a supported state (all 9 buttons can be enabled today), and one tap in Settings turns it off. **Wiring:** the standard 6 hideable-section touch points, plus watchlist blocks in all four sync paths (`pushPersonal`/`applyPersonal`/`pushHousehold`/`applyHousehold`), a `_rDirty.watchlist` flag with a `!_watchEditing` guard in `flushSyncRenders` (an incoming sync must not redraw a half-typed entry away), a `closeTopOverlay` branch so the phone back button offers "Discard changes?" from the editor (v345 pattern), `watchlist` in `buildExportPayload` + an additive restore block in `importBackupData`, and the export coverage comment updated. **Deliberately NOT built:** no Home card (Home is already long, and unlike Plants this section has a permanent nav slot), no global-search group, no poster art or any image (photos are the one thing that threatens the localStorage budget — v377's standing constraint), no watched-date display beyond the sort order. **Tests: new `tests/cases/30-watchlist.js` (49 assertions), no existing test file touched** — the merge contract in both directions incl. field-fill, cleared-fields, nulls on either side and the tombstone/re-add pair; `watchInfoLinks` encoding (incl. a `Tom & Jerry?` title that must not break out of the query string) and the no-title case; `watchRatingOf` clamping junk to 0 rather than NaN; `getWatchlist`'s source-level null guard; the tick stamping `watchedAt`+`updated`; same-star-clears-rating; out-of-range ratings refused; **un-ticking preserving the rating**; the -1 guard (toggle/rate/delete against a missing id is a no-op, not a crash); delete writing a tombstone; export coverage incl. the rating riding along; and the render itself — watched group collapsed by default and revealed on open, info links absent from collapsed rows but present (with `rel=noopener`) when expanded, chips hidden when only one kind exists, a vanished filter falling back to All, the empty state, and **a title containing `<img src=x onerror=…>` rendering as text rather than an element**. Verified non-vacuous twice, each failing exactly its own assertion and nothing else: reverting `getWatchlist`'s null filter fails only "getWatchlist drops null entries"; reverting the `esc()` on the title fails only the markup assertion, with `img count: 1`. Restored, re-confirmed **33/33 files green**. Verifier: SAFE TO COMMIT.
- **v390:** **Fix: `applyPersonal` never paused outgoing sync while merging, unlike `applyHousehold` (Cathal's ask 03/08/2026, sixth and last item of the bug queue; build-ready for plain Sonnet — the design call was already made and deliberately deferred at v375, see that changelog entry).** `applyHousehold` has set `syncPaused = true` on entry (before any merge work, cleared in `finally`) since ~v375. `applyPersonal` only ever cleared `syncPaused` in its `finally` — it never set it true on entry — a real inconsistency v375 flagged and explicitly declined to fold into that release ("changing WHEN outgoing sync is paused alters sync timing... wants its own version to be testable in isolation"). The gap: `pushPersonal()` bails early whenever `syncPaused` is true, so a push that fired *while* `applyPersonal` was still mid-merge (rather than strictly before or after the whole cycle) was never held back for it — exactly the race `applyHousehold` has been immune to for several versions. **Fix:** one line — `syncPaused = true;` added at the top of `applyPersonal`, right after its guard clause, mirroring `applyHousehold`'s entry exactly. The existing `finally { syncPaused = false; }` (added in v375, alongside the whole-function try/catch) needed no change — it already resets the flag unconditionally, throw or no throw. No data-model, export, or sync-protocol changes; this only narrows *when* outgoing pushes are held back, never what gets merged. **Tests:** new `tests/cases/29-sync-paused-parity.js` (5 assertions). Couldn't be pinned through `pushPersonal()` itself — its very first guard is `if (_isTestBuild) return;`, and every `file://` test page is a test build, so the push path never reaches the `syncPaused` check inside this harness. Assertions instead pin the actual state transition the fix makes, by hooking `mergeBabyData` (a function genuinely invoked partway through the merge) to capture `syncPaused`'s value at that exact moment: `applyPersonal` now reads `syncPaused === true` while a merge block is actually running and `false` again once the cycle completes; a throw mid-merge (simulated by making the hooked function throw) still clears it via the existing `finally` — the v375 guarantee, preserved under the new earlier `true`; and a direct side-by-side confirms `applyPersonal` and `applyHousehold` now produce identical during/after shapes. Verified non-vacuous: reverting the one-line fix and re-running fails exactly the two assertions expecting `duringMerge === true` on `applyPersonal`, with `applyHousehold`'s own during-merge reading unaffected (proving the test isolates the right function); restored, re-confirmed **32/32 files green**. Verifier: SAFE TO COMMIT. **This closes the bug queue Cathal opened 03/08/2026** (① recipe null-crash → v385, ② recipe-search state bugs → v386, ③ recipes-in-global-search → v387, ④ normaliseNotes null guard → v388, ⑤ sw.js cache staleness → v389, ⑥ this item → v390) — nothing outstanding from that list remains.
- **v389:** **Fix: `sw.js`'s `addAll` staleness window (Cathal's ask 03/08/2026, fifth of the bug queue; build-ready for plain Sonnet per the Pending entry, which already scoped the exact one-line fix).** `install` ran `c.addAll(ASSETS)`, and those fetches went through the normal browser HTTP cache — `updateViaCache: "none"` on the registration exempts only `sw.js` itself, not the assets it fetches. GitHub Pages serves `index.html` with a short `max-age`, so a new SW installing inside that window could populate the freshly-named `hearth-vNNN` cache with the **previous build's** `index.html` — cache name says vNNN, contents are vNNN-1. It self-heals on a later open via the fetch handler's existing background refresh, but costs another open or two, and is a second independent reason a version bump can appear not to land (distinct from the v382 falsely-fresh-label bug). **Fix:** `c.addAll(ASSETS.map(u => new Request(u, { cache: 'reload' })))` — forces every install fetch past the HTTP cache, exactly the one-liner the Pending entry had already scoped. **Not verified against live GitHub Pages headers** — same caveat the Pending entry carried forward: this sandbox has no outbound access to `coshea321.github.io` (confirmed again this session via both a direct `curl` and the `WebFetch` tool, both 403). The fix is safe regardless of the actual header value though — forcing a fresh fetch during install can only ever be *at least* as fresh as relying on the HTTP cache, never worse, so shipping without live confirmation carries no real risk. No data-model, export, or sync changes. **Tests: none added, and that's a deliberate call, not an oversight.** The SW test server (`tests/server.js`) always sends `Cache-Control: no-store` on every response by design — `02-sw-update-banner.js`'s own comment says the update tests *depend on* the browser re-fetching `sw.js` rather than reusing an HTTP-cached copy, so weakening that global default to model GitHub Pages' real caching behaviour risks flaking existing coverage. A faithful test would need a new opt-in server mode (real `Cache-Control`, changing response bodies between fetches, browser HTTP-cache priming) — real infrastructure, disproportionate to a one-line, standard-Fetch-API-idiom fix. `node tests/run.js` re-run as-is: **31/31 files green** (no regression). Verifier: SAFE TO COMMIT.
- **v388:** **Fix: `normaliseNotes()` had no null guard (Cathal's ask 03/08/2026, fourth of the bug queue; build-ready for plain Sonnet, no design call needed).** Found while building v384's global search — `n.body`/`n.title` were read straight off each entry with nothing checking `n` itself first, so a `null` in `fl4_notes_global`/`fl4_notes_global_work` (sync merges and bad writes are both capable of producing one) would throw. v384 narrowly worked around it **only inside `globalSearchNotesPool()`**, filtering raw nulls before handing the array to `normaliseNotes()`, and explicitly logged the function itself as still-exposed for every other caller — the Notes section's own rendering (`renderNotesSection()`), opening or highlighting a note (`openNoteById`, `toggleNoteHighlightById`), and the Family Log event aggregator (`renderFamlog()`) all called `normaliseNotes()` directly on the raw store with no pre-filter. **Fix is at the source, not the symptom** — the same shape as the v385 `getRecipeBook()` fix: `normaliseNotes()` itself (index.html ~line 7817) now filters non-object/null entries before mapping, so every caller gets clean data from one place. `globalSearchNotesPool()`'s now-redundant local pre-filter and its explanatory comment were simplified away rather than left as dead-weight duplication of the same guard. No data-model, export, or sync changes — nulls are garbage entries. **Tests:** new `tests/cases/28-normalise-notes-null-guard.js` (9 assertions) — `normaliseNotes()` against `null`/`undefined`/a null entry/multiple leading nulls; the Notes section itself rendering with a null present and still showing the good note; `openNoteById()` and `toggleNoteHighlightById()` both surviving a null entry; the Family Log aggregator surviving a null in either notes store. Verified non-vacuous: reverting the one-line filter fails both this new test file **and** `tests/cases/26-global-search.js` (since its local workaround was simplified away, it now depends on the source-level fix) with the exact `TypeError: Cannot read properties of null (reading 'body')` the fix prevents; restored, re-confirmed **31/31 files green**. Verifier: SAFE TO COMMIT.
- **v387:** **Global search phase 2 — Recipes (Cathal's ask 03/08/2026: "add recipes to global search"; third of the bug-queue/feature session; build-ready for plain Sonnet per `HEARTH-global-search.md` §8/§9, which had already flagged this as "his call, not a build decision" and left the mechanics scoped).** `GLOBAL_SEARCH_GROUPS` gains a `recipes` entry (🍽, themed `#B5651D` to match the Recipes section); `globalSearchPool("recipes")` reads `getRecipeBook()` (already null-guarded at the source since v385 — no extra guarding needed here); matching reuses v383's **wider** `recipeSearchText(r)` (name, notes, category, method, ingredients) via a new small dispatcher `globalSearchText(key, item)`, rather than the plain `listSearchText` every other group uses — so a query like "penne" or "scrambles" that only a list item's name+notes could never answer now works through global search too, exactly the "which recipe used the chorizo?" case the design doc named as the reason Recipes was the obvious phase 2. **Colour handling refactored slightly:** `GLOBAL_SEARCH_GROUPS` entries now each carry their own `color` (copied through into `globalSearch()`'s returned groups), replacing the render-time `LIST_TYPES[g.key].color` lookup — Recipes isn't a `LIST_TYPES` entry, so the old lookup would have thrown; every existing group's colour is unchanged, just sourced from one place instead of two. **The hand-off needed a new `openRecord("recipes", null, opts)` branch** — Recipes wasn't built in v384 (explicitly deferred, `HEARTH-global-search.md` §4 table). It resets `_recipeFilter` to `"all"` (never silently AND a hand-off with a stale category chip, the v381/v386 bug class) and pre-fills `_recipeSearchQuery` from `opts.query`, opening the search row/input the same way the existing Lists branch does. **The one genuinely tricky bit:** v386 (shipped minutes earlier, same session) made `switchSection`'s `"recipes"` branch unconditionally clear `_recipeSearchQuery` on every arrival, to fix "leaving mid-search and returning leaves a stale search." A naive `openRecord` hand-off would have set the query and then immediately had it wiped by that same reset. Fixed with a one-shot flag, `_recipeSearchPreset`: `openRecord` sets it `true` immediately before calling `switchSection("recipes")`; the arrival-reset checks it first — if true, it consumes the flag and skips the reset just this once; any later, ordinary arrival (nav tap, back button) still gets the full v386 reset. No data-model, export, or sync changes. **Explicitly still out of scope** (per the design doc, unchanged by this version): Trips/Plants/Baby/Medicine/Track remain excluded; no id-level deep link into a single recipe (lands on the Recipes list with the query applied, same as every other group). **Tests:** extended `tests/cases/26-global-search.js` (46 → 58 assertions, no new file) — a new `RECIPES` fixture (including a trailing `null`, mirroring the known `fl4_recipebook` bug class) pins that "milk" finds a recipe via an **ingredient** match (not name/notes), that "penne" — zero name/notes overlap — still finds Chorizo Pasta (proving the wider haystack is actually used), that the null entry doesn't crash the group, that a real "curry" hit now legitimately appears in *both* grocery and recipes (updated from the v384 fixture, which only had a grocery hit), and the full hand-off: a stale category filter reset, the query pre-filled into the Recipes search row/input, the right recipe shown, and — the interaction-fix-specific case — that a LATER ordinary arrival at Recipes (not via `openRecord`) still gets the v386 stale-search reset, i.e. the one-shot flag doesn't get stuck true. Verified non-vacuous three ways: reverting the `GLOBAL_SEARCH_GROUPS` entry crashes the test file outright (recipes group `undefined`); reverting the `_recipeSearchPreset` check alone fails exactly the two hand-off-prefill assertions with the query landing as `""`; both restored, re-confirmed **30/30 files green**. Verifier: SAFE TO COMMIT.
- **v386:** **Fix: two recipe-search state dead ends left by v381 (Cathal's ask 03/08/2026, second of the bug queue; build-ready for plain Sonnet, no design call needed).** v381 fixed search silently AND-ing with a stale Favourites filter, but only in the direction of *opening* search — two other doors to the identical symptom (a real match hidden by a leftover filter, reading as "search does nothing" with no visible cause) were left open, both confirmed by headless repro before fixing. **(a) A category chip tapped while a search was active silently re-ANDed the two** — searching "curry" then tapping "Dinner" (say) could show "Nothing here yet." even with Chicken Curry sitting right there, because the chip only changed `_recipeFilter` and left `_recipeSearchQuery` armed. Fix: the `.rcp-filter` click handler (`renderRecipes()`, ~line 5479) now also clears `_recipeSearchQuery`, hides `#rcpSearchRow` and empties `#rcpSearchInput` whenever a search was active — mirroring v381's fix in the opposite direction (that one resets the filter when search *opens*; this resets the search when a chip is tapped). **(b) Leaving Recipes mid-search and coming back left the query armed and the search row open** — the list read as mysteriously filtered on return with no way to tell why. Fix: `switchSection`'s `"recipes"` branch (~line 10840) now resets `_recipeSearchQuery` + hides the row + clears the input on every fresh arrival, the same pattern v384 already established for the Home search row (`_gSearchQuery` cleared in `switchSection`'s `"home"` branch, not in the renderer itself). **Deliberately NOT inside `renderRecipes()`** for either fix — that function also runs on a chip tap from a *different* origin and on a sync-triggered redraw while already on the screen, neither of which should wipe an in-progress search; the resets live at the two actual trigger points (a chip click, a fresh section arrival) instead. No data-model, export, or sync changes — this is DOM/render-state wiring only. **Tests:** extended `tests/cases/25-recipe-search.js` (18 → 26 assertions, no new file) — a chip tap while a real match is being hidden by a stale zero-match search correctly clears the query/row/input and shows the chip's own recipes; leaving Recipes (`switchSection("home")`) mid-search and returning (`switchSection("recipes")`) correctly clears the same three and shows the whole book, not a filtered remnant. Verified non-vacuous: reverted each fix independently and re-ran — (a) reverted alone fails its 4 assertions with the list still reading "No recipes match "zzzzqqq"." after the chip tap; (b) reverted alone fails its 4 assertions with the query still `"curry"` and only 1 of 12 recipes showing on return. Restored both, re-confirmed **30/30 files green**. Verifier: SAFE TO COMMIT.
- **v385:** **Fix: a `null` entry in `fl4_recipebook` crashed the entire Recipes section (Cathal's ask 03/08/2026 — first of a queue of recently-found bugs being worked through one by one, alongside adding Recipes to global search; build-ready for plain Sonnet, no design call needed).** `renderRecipes()`'s category-collection loop did `(r.category||"").trim()` with no null guard, so one bad record (sync merges and CSV import can both produce one) threw before anything rendered — the section went fully dead, not merely missing one row. **Fix is at the source, not the symptom:** `getRecipeBook()` itself (index.html ~line 2533) now filters out null entries before returning, rather than patching `renderRecipes()`'s loop alone — the Tidy view, Week planner and Grocery-pick screen all call `getRecipeBook()` independently and would otherwise still crash on the exact same corrupt store. One line changed. No data-model, export, or sync changes — nulls are garbage entries, nothing legitimately depends on one being present. **Tests:** new `tests/cases/27-recipe-null-guard.js` (6 assertions) — `getRecipeBook()` drops a null; the list view renders and still shows the good recipe alongside a trailing OR a **leading** null (worst case for a naive filter); the Tidy and Week-plan views, which fetch the book independently, both survive too. Verified non-vacuous: reverted the one-line fix and re-ran — 5 assertions fail with the exact `TypeError: Cannot read properties of null (reading 'category')` (list view) and `reading 'ingredients'` (Tidy view) the fix prevents; restored and re-confirmed 29/29 files green. `node tests/run.js`: **29/29 files green**. Verifier: SAFE TO COMMIT.
- *(v318–v383 + the 12/07 docs-housekeeping entry + the two council write-ups + the two 19/07 Fable no-bump entries (audit, design day) + the 27/07 app-privacy research note moved to `HEARTH-archive.md`, latest move 03/08/2026 (v384 moved to make room for v394) — keep-newest-~10 rule.)*

## Recipes section — current shape
- Store: `fl4_recipebook` — **shared household-wide** (newest-wins-by-`updated` merge via `/shared`, plus same merge on the personal channel for same-account devices); separate from grocery-import `fl4_recipes`.
- Section headers: `<b>…</b>` / "Label:" lines render as sub-headings in the detail view and are excluded from the grocery list (`isHeaderIng`).
- List view: filter bar (All / ★ Favourites / per-category chips), grouped by category in All view.
- Detail view: servings stepper, ingredients (scaled per unit rules, **bulleted since v394**), method (**auto-numbered `<ol>` since v394 — `methodSteps` splits it, the stored text is never rewritten**), notes, **source link (v393, optional — shown only when set)**, fav star, **🛒 Add to grocery list…** (opens the v368 review screen, see below), Edit/Delete. A line whose shopping preference is set shows it quietly ("· not added to the shopping list" / "· bought as “X”").
- Editor: name, serves, category (datalist-suggested), **in rotation checkbox (v362, eligible for weekly auto-fill)**, ingredients (one per line "amount unit name"), method, notes, **URL (v393, optional — auto-prefixed with `https://` via `normalizeRecipeUrl`)**.
- Import: ⬇ Import button → paste CSV → live "N recipes found" preview → Import.
- Unit rules: tsp/tbsp fixed; cups→g (dry) or ml (liquid); counts pluralise.
- Add-to-grocery: tag = recipe name; **amounts** carried as a separate `item.amount` field on grocery items, scaled to current servings (names-only fallback when no clean amount); saves a reusable chip at base servings.
- Week plan (v317): third recipe view ("📅 Week plan" button on the list) — 7 day cards (today..+6, dinner only), tap-to-assign from a favourites-first picker, per-day servings, "Add week to groceries" batch-calls the per-recipe flow with summed servings. Store `fl4_mealplan`, household-shared (newest-wins per day, no tombstones).
- **Canonical ingredient shape (v366): `"<amount> <unit> <Name> (<detail>)"`.** No per-ingredient note field exists or is needed — the bracket IS the note channel, because `cleanGroceryName` strips brackets out of the grocery name and `amtDetail` carries the text into the item's note. Anything new that writes an ingredient should emit this shape. `ingredientToLine(ing)` is the single serializer (headers re-emit as `<b>Name</b>`, fractions as fractions) and `parseIngredients` reads it straight back.
- **Tidy tool (v366): recipe list → 🧹 Tidy.** One-off maintenance pass that rewrites STORED ingredients into the canonical shape (everything before it — v363 cleanup, v365 rescue — was downstream-only and never touched storage). Review-first: tick per recipe, edit any line inline, nothing written until Apply; a line it can't rewrite losslessly is flagged "needs your eye" and left byte-identical. Step two offers to unify names that mean the same product. See the v366 changelog entry for the full safety model.
- **Grocery review screen + per-ingredient shopping preferences (v368).** "🛒 Add to grocery list…" opens a review step: tick what you need, and edit the **shopping name** (the name that lands on the list) per line. Both are stored on the ingredient — `groceryName` (override, used verbatim; wins over `splitGroceryName`'s derived name) and `noBuy` (never shop for this) — and remembered for every later add. **`groceryName` deliberately never rewrites the ingredient's own `name`**, so the recipe keeps its wording and nothing can be lost by a rename; `noBuy` is honoured on every path into the list (single recipe, week planner, roll-over) because they all go through `addRecipeToGroceries`. Read them with `recipeGroceryName(ing, sp)`. **Anything that re-parses ingredient text must call `carryGroceryPrefs(next, prev)`** or both fields are silently dropped (already wired into the editor save, `applyTidyLines` and `rescueIngredientMeasure`).
- Rotation + roll-over (v362): recipes carry `inRotation`/`lastCooked`; "🔁 Fill empty days" (planner button) and a weekly "New week — plan dinners?" banner both call `runMealPlanFill()`, which assigns least-recently-cooked in-rotation recipes to empty days in the window only — never auto-fires, always behind a tap. See the v362 changelog entry for full detail.

## Pending / next steps (not yet built)
- **Watchlist follow-ups (v391 shipped the core; each needs a design confirm).** ① **"Who wants to watch it"** — offered to Cathal in the v391 design questions (tag each entry Cathal / Petra / Both, with a filter, so picking something on the night is easier) and **not** picked. Not rejected on philosophy, just not chosen for v1 — build it if he asks, don't volunteer it. ② **A Home/Today-card line** ("🍿 3 to watch") — deliberately skipped because Watch has a permanent nav slot, unlike Plants; only worth revisiting if he turns the nav icon off and still wants a way in. ③ **Global search group** — cheap to add (`GLOBAL_SEARCH_GROUPS` entry + a `globalSearchPool` branch, exactly the v387 Recipes shape) since the records already carry searchable `title`+`note`; left out of v1 because the list is short enough to eyeball. ④ **Watched-on date shown** — `watchedAt` is already stored and only drives the sort order today. ~~⑤ Seasons/episode progress as a structured field~~ — **half-shipped v392** (03/08/2026: an optional TV-only `season`, clamped 1–99; **episodes explicitly not wanted** — Cathal's words were "episode not needed", so do NOT add episode tracking without a fresh request). ⑥ **A "next up" or last-watched date on the Watching group** — the group sorts by `updated`, which is a proxy; a real "last watched" stamp would need a new field and a reason to want it.
- ~~**Bug queue, 03/08/2026 (Cathal: "add recipes to global search and let's tackle all the recently found bugs one by one")**~~ — **CLOSED, all six shipped:** ① recipe null-crash → v385, ② recipe-search state bugs → v386, ③ recipes-in-global-search → v387, ④ normaliseNotes null guard → v388, ⑤ sw.js cache staleness → v389, ⑥ applyPersonal syncPaused parity → v390. Nothing outstanding from this queue.
- **Two update-delivery / search follow-ups deferred out of v382 (offered to Cathal 02/08/2026, he took the label fix only — both are still real).** ~~① `sw.js` `addAll` staleness window~~ — **shipped v389** (03/08/2026: `c.addAll(ASSETS.map(u => new Request(u, { cache: 'reload' })))` — see the v389 changelog entry above; still not verified against live GitHub Pages headers, this sandbox has no outbound access to the Pages host, confirmed again this session, but the fix is safe regardless of the actual header value). ~~③ A `null` entry in `fl4_recipebook` crashes the whole Recipes section~~ — **shipped v385** (03/08/2026: root-cause fix in `getRecipeBook()` itself, not just `renderRecipes()` — see the v385 changelog entry above). ~~② Two recipe-search dead ends v381 left behind~~ — **shipped v386** (03/08/2026: chip-tap-while-searching now resets the search, and a fresh arrival at Recipes now resets a stale search left over from a previous visit — see the v386 changelog entry above). The deeper empty-state fix ("Nothing here yet." should say *why*) was already shipped in v383; both v386 fixes reset state at the trigger point instead, so the same-cause empty state simply doesn't arise any more in these two cases.
- ~~Global search~~ — **shipped v384** (03/08/2026: 🔍 row on Home, `globalSearch()` over Lists+Notes, `openRecord()` deep-link helper, per `HEARTH-global-search.md`); **Recipes phase 2 shipped v387** (03/08/2026, see that changelog entry). Still open per the design doc's §9: **item-level deep links** (needs new design). ~~**`normaliseNotes()` has no null guard**~~ — **shipped v388** (03/08/2026: fixed at the source in `normaliseNotes()` itself, not just worked around locally in `globalSearchNotesPool()` — see the v388 changelog entry above).
- **Plants follow-ups (v377 shipped the core; these were deliberately left out, each needs a design confirm).** ① **Seasonal intervals** — `waterDays` is a single number, but the real advice is "weekly in summer, every 2–3 weeks in winter"; the write-up carries that in prose today. A summer/winter pair with an automatic switchover is the obvious shape, but it doubles the scheduling code and Cathal may prefer just editing the number twice a year — ask before building. ② **A "water all" bulk action** on the list, for the realistic case of doing the whole flat in one go. ③ **Care history** — the logs hold up to 40 entries each and nothing currently shows them; a small "last 5 waterings" list in the detail view is nearly free. ④ **A second photo** (e.g. a before/after for a plant being nursed back) — deliberately capped at one for now because photos are the only thing in Hearth that threatens the localStorage budget; revisit only if the 1.5MB warning never fires in practice. ~~⑤ import/export~~ — **shipped v378** (02/08/2026: `plant-v1` files, two-step importer, AI prompt template). ~~⑥ area tags + a Home link~~ — **shipped v379** (02/08/2026: `room` reused as the area, chip filter on the list, permanent Home glance card). Anything that only adds a section to `PLANT_SECTIONS` or changes copy is build-ready for plain Sonnet.
- **Startup freeze / "sometimes doesn't open" — PLAN COMPLETE, Releases A–E all shipped (from `HEARTH-freeze-analysis.md`), and its one remaining follow-up is also now closed.** History: external review verified against v371 (31/07/2026, Fable); ~~Release A~~ — **shipped v372** (debug overlay gated, `_bootUpdateInstalling` reset, reconnect guards, error message) — Cathal reported it alone was already a big improvement; ~~Release B~~ — **shipped v373** (trade-off confirmed by Cathal via AskUserQuestion, 31/07/2026: instant open + update banner instead of the blocking boot gate / boot reload / entry watchdog; SW shell now cache-first with background refresh — see the v373 changelog entry); ~~Release C~~ — **shipped v374** (`fetchWithTimeout` helper, one shared AbortController wrapper ~8s, applied to the 12 bare fetch sites in the sync/auth stack — see the v374 changelog entry). ~~Release D~~ — **shipped v375** (3 design calls confirmed by Cathal via AskUserQuestion, 01/08/2026: dirty-flag batching via the new `flushSyncRenders()` helper with the existing per-block guards preserved inside it, *plus* a `currentSection === "lists"` guard on `renderList()`; whole-function try/catch on `applyPersonal` matching `applyHousehold`, plus a per-render inner catch; `syncPaused` parity deliberately left out — see the v375 changelog entry). ~~**Follow-up left open by that decision: `applyPersonal` still never sets `syncPaused = true` while it merges, unlike `applyHousehold`**~~ — **shipped v390** (03/08/2026, see that changelog entry above). ~~Release E~~ — **shipped v376** (3 design calls confirmed by Cathal via AskUserQuestion, 01/08/2026: always-on second phase in the same `node tests/run.js` command; scope = SW registration/cache + version-label round trip + update banner both ways + dead-network opens; and the SW made to register by serving the repo locally **under the production hostname in the test browser only**, since the analysis's literal "localhost mode" cannot work against the app's own `_isPreview`/`_isTestBuild` guards — no test scaffolding was added to `index.html`. See the v376 changelog entry, including the negative control that proves the offline case actually fails on a network-first shell). **The whole freeze plan, including its one follow-up, is now closed.**
- ~~**Run the tidy pass at CSV import time (the v366 council's recorded dissent, 29/07/2026)**~~ — **shipped v367** (30/07/2026): `recipesFromCSV` now runs every parsed ingredient through `tidyIngredient()`, keeping only confident rewrites; the import preview shows "N recipes found · M ingredient lines tidied". See the v367 changelog entry.
- ~~**Case-only merge groups are busywork (v366 council item ④)**~~ — **built in v366's third pass** (29/07/2026, Cathal asked for it directly): the `tidyPickCommon` button resolves every non-synonym group to its most common spelling in one tap, and deliberately leaves the synonym groups alone. Nothing preselected, so "show duplicates, I decide" still holds.
- ~~Re-adding a recipe at different servings doesn't refresh the Amount field it wrote itself~~ — **shipped v370** (31/07/2026, full detail in the changelog entry above). Fix was exactly as scoped: the `_recipePrev` snapshot is now also taken in the NEW-item branch (`amount: ""`), so a later re-add correctly recognises the field as recipe-owned. Nothing left open on this item.
- **Private access via Cloudflare Pages + Cloudflare Access (raised 27/07/2026; direction chosen, NOT yet started — build-ready for a plain-Sonnet session per the design-here-build-on-Sonnet handoff).** Goal: anyone who finds the Hearth URL gets a login wall instead of the app; family members get in with an emailed one-time PIN. Cost: **£0** on Cloudflare's free tier (Pages: unlimited sites/bandwidth; Access: free to 50 users — Hearth needs ~4).
  - **Decisions already made — do NOT re-litigate these.** (1) Cloudflare, not GitHub Pro/Enterprise, Netlify or Vercel — reasons in the 27/07 write-up above. (2) **The repo STAYS PUBLIC.** This is the whole reason Cloudflare wins: the *site* gets gated while the repo stays open, so the `raw.githack` "👉 Try this version" PR test links (v324, core to how Cathal reviews) keep working. Making the repo private would break every one of them — githack serves public repos only. (3) No `robots.txt` (inert on a project site). (4) No in-app PIN gate — the source is downloadable, so it is security theatre. (5) The existing `noindex` tag at `index.html:5` stays exactly as it is.
  - **Split of work.** Almost all of this is **Cathal clicking in the Cloudflare dashboard** — a Claude session cannot do it for him. The session's job is to walk him through it step by step, then handle the small repo-side follow-ups. Budget the session for hand-holding, not coding.
  - **Steps.** (1) Cathal creates a free Cloudflare account. (2) Cloudflare Pages → *Connect to Git* → pick `coshea321/miniature-journey`, production branch `main`, **framework preset: None, no build command, output directory `/`** (Hearth has no build step — getting this wrong is the most likely stumble). This gives a `<project>.pages.dev` URL that rebuilds on every merge to `main`, exactly like Pages does today. (3) Confirm the app actually works from that URL *before* adding any gate — check the service worker registers, the manifest loads, and login to Firebase still works from the new origin. (4) Zero Trust → Access → Add an application → **Self-hosted**, pointed at the Pages hostname. (5) Policy: action *Allow*, include **Emails** → the family's addresses, one per person. Identity provider: **One-time PIN** (no Google/GitHub login needed — Cloudflare emails a code that expires in 10 minutes). (6) Set the session duration long (a month) so the PWA is not constantly asking for a PIN. (7) Only once it all works: repoint whichever URL the family has on their home screens, and decide whether to switch the old GitHub Pages site off.
  - **Open questions for Cathal — ask these first, do not assume.** (a) **Custom domain?** Protecting a production site cleanly generally wants a domain on Cloudflare (~£10/yr) — the one place a real cost appears. Confirm whether he'll buy one or wants to try to gate the bare `pages.dev` hostname first. (b) **Keep GitHub Pages running in parallel** during the changeover, or cut straight over? (c) Which email addresses go on the allowlist?
  - **Known caveats to state honestly up front, not discover later.** The PWA gains a login gate: once installed, the service-worker cache still serves offline, but when the Access session expires the next online open needs a PIN email — new friction that does not exist today. **Everyone must re-add the app to their home screen from the new URL**, and a new origin means **fresh `localStorage`** — so the Firebase URL, login, and household code must be re-entered on every device (export a backup first; nothing carries across origins automatically). This is the biggest practical cost of the move and Cathal should know it before step 1, not after.
- ~~**Recipe-parser second pass**~~ — **shipped v365** (28/07/2026, full detail in the changelog entry above). Design chat done same day on Fable via PR #131, all 4 decisions built exactly as confirmed — scope = compound amounts + ranges only (alternatives dropped, multi-unit brackets skipped), existing data handled via rescue-at-render (not migration), grocery amount chip shows the scaled range string. Nothing left open on this item.
- ~~**Meal-plan rotation + weekly roll-over**~~ — **shipped v362** (27/07/2026, full detail in the changelog entry above). Both design calls built exactly as confirmed 27/07/2026 — rolling day-key window kept (no ISO-week re-key), roll-over is prompt-first (banner, nothing mutates shared data without a tap). `inRotation`/`lastCooked` recipebook fields, the Fill algorithm, bulk-onboarding via select mode, the ✓-cooked control, and the weekly banner are all built; nothing left open on this item.
- **Bug-review fix queue (in-depth review at v351, PR #113 — triaged on Fable 25/07/2026; all decisions below are CONFIRMED, don't re-open them. Full evidence + exact line anchors live in `HEARTH-bug-review-2026-07-25.md` at repo root — the building session should read the relevant finding there before editing. Line numbers are at v351; re-grep before editing.)** Six versions, one PR each, build in order A→F (A is the data-loss fix, F is polish). **Every batch is BUILD-READY for plain Sonnet per the 12/07 handoff policy** — the sync-touching design calls were settled in the triage session. Where a test case is named, ship it in the same PR and call out the `tests/` diff in the PR body per policy.
  - ~~**A (build first — data loss): findings 1 + 12 — `getWD()` restore + sync carry-through.**~~ — **shipped v352** (25/07/2026, full detail in the changelog entry above). `importBackupData` now merges `bodyweight` by `date` and `bp` by `ts` (additive union, keep-local on collision, as decided) and mutates the local `getWD()` object in place rather than rebuilding from a hand-typed field list; `applyPersonal`'s rebuild is now based on `remoteWD`+`localWD` (was `remoteWD` alone), so unknown top-level keys carry through from whichever side has them. Closes sync-audit item ③ below. `07-backup-roundtrip` extended to 12 assertions (was 7).
  - ~~**B: finding 2, all four sites — unstamped mutations lose the newest-wins merge.**~~ — **shipped v353** (25/07/2026, full detail in the changelog entry above). All four sites (reopen-all, recipe re-tag ×2 twins, `doClear` ×2 branches, note star toggle) now stamp `updated`/`updatedAt`. `09-mutation-stamps` added (9 assertions).
  - ~~**C: findings 6 + 5 — Train session overlay unreachable by the phone back button, and native `confirm()` in its exit path.**~~ — **shipped v354** (25/07/2026, full detail in the changelog entry above, PR #117). **(6)** `sesBackBtn`'s two native `confirm()` calls converted to `confirmDialog()`. **(5)** `closeTopOverlay()` now clicks the existing `#sesBackBtn` when `#sessionOverlay` is display-visible, so the exit-confirm guard still applies. Manual on-device check via the test link caught a gap: physio sessions had no confirm branch at all — **fixed as v355** (see the changelog entry above).
  - ~~**D: finding 3, LOCAL half only — growth inline date edit breaks one-entry-per-date.**~~ — **shipped v358** (26/07/2026, full detail in the changelog entry above). Save handler refuses a colliding date-change with a toast (editor stays open); delete converted to an index-based `splice` via a per-row `data-gidx` so a merge-created duplicate can be cleaned up one row at a time. `11-growth-date-edit` added (8 assertions). The cross-device half of finding 3 stays inside the gated growth-sync-hardening item below, untouched.
  - ~~**E: finding 4 — a nameless hist entry still crashes four readers.**~~ — **shipped v359** (26/07/2026, full detail in the changelog entry above). The same `if (!h || !h.name)` skip added to `deleteHistEntries`, `showAC` (was throwing on EVERY keystroke with a corrupt entry present), `addToHist`, `renderHistory`, `addToHistFor`; `buildGrocerySuggestions` already guarded, left unchanged. `12-nameless-hist-readers` added (8 assertions).
  - ~~**F: polish/hardening bundle — findings 7, 8, 9, 10, 11, 14, 15, 16.**~~ — **shipped v360** (26/07/2026, full detail in the changelog entry above). This was the last batch in the queue — A→F all shipped, PR #113's fix list is closed. `13-trip-import-unique-ids` added (8 assertions).
  - **NOT being built (decided in the triage — do not re-chase without a fresh Cathal request):** finding 13's side-store move (`_recipePrev`/`_recipeAdded` stay on the item — the clear/revert flow needs them to survive a reload; now documented under Data & export instead); a separate star-stamp field for notes (see 2d); any change to the merge tie-break direction (see Open decisions); the cross-device half of finding 3 (stays inside the gated growth-sync-hardening item below).
- ~~**Duplicate workout-session functions — investigate which copy is live**~~ — **shipped v351** (25/07/2026, full detail in the changelog entry above). Verdict on the open question: inert dead code — all 24 pairs byte-identical, dead copies deleted surgically (7 interleaved live helpers kept), plus the legacy sync-state duplicate declarations (`fbUrl`/`syncRoom`/`syncPaused`/`lastPushTs`/`syncPrefs`); CI `ALLOWLISTED_DUPES` emptied so any future duplicate fails the build.
- **Sync audit fix list (Fable audit 19/07/2026 — full findings in the archive's Fable-audit entry):**
  - ~~① Notes sync hardening~~ — **shipped v343** (19/07/2026, PR #102, full detail in the changelog entry above). Same-name notes fields (`updatedAt||id` newest-wins, tombstones), NOT the `updated` field name the original spec sketch used — `mergeNotes`/`notesTs` match the notes objects' real shape.
  - ~~② Work notes join sync~~ — **shipped v343** (same PR — built together with ① since both touch the same four call sites). Same `isSynced("notes")` gate as Personal notes, as recommended; flagged as an explicit call in the PR body per the recommendation note.
  - ~~③ `applyPersonal` generic-key carry-through~~ — **shipped v352** (batch A of the bug-review fix queue, full detail in the changelog entry above). *(The other half of this item — the duplicate `var lastPushTs` — was deleted in v351.)*
- ~~**Grocery autocomplete misses items visibly in the list — hist/items divergence ("blueberries" incident)**~~ — **shipped v348** (21/07/2026, PR #108). Both parts built as designed in PR #107: `showAC` items fallback + `capHistToLiveLimit` cap protection at all 4 prune sites. Full detail in the v348 changelog entry above; out-of-scope calls from the design (hist count reconciliation, raising the cap, feeding hist from quick-pick/recipe revives) remain parked, unchanged.
  - **Model economy:** the build itself is fine on plain Sonnet from this entry — a well-defined read-side change plus a contained cap guard, no fuzzy design left. One version, normal verifier pass; manual check = type a Done item's name in grocery and see it suggest.
- ~~Body weight & BP trend charts + Body-first sub-tab~~ — **shipped v336** (18/07/2026, full detail in the changelog entry above). Edited the live `renderBodyView` copy only, per the standing dead-duplicate gotcha; the duplication itself remains open as council item ④ below.
- ~~Notes toolbar "?" legend~~ — **shipped v337** (18/07/2026, PR #90, full detail in the changelog entry above).
  - Build note: shipped as its own version/PR per the one-PR-per-version policy — keeps each diff small and independently testable via the raw.githack test link.
- **Council fix-first list (whole-app review at v327, 16/07/2026 — full detail in the changelog entry above; do these before the next feature version):** ~~① DOMPurify-missing fallback → `esc()` — **shipped v328**~~; ~~② medicine-array per-entry union merge — **shipped v329** (design confirmed + built same day)~~; ~~③ widen `.ppc` per-person circles toward ~44px effective targets — **shipped v331**~~; ~~④ `openSetSheet` dedupe when Train is next touched~~ — **shipped v339** (the whole dead duplicate Train block, setSheet copies included, removed in the Track build); ⑤ `MED_RULES` brand-name extension — **RESOLVED 19/07/2026: Cathal photographed the actual bottles.** Baby products = Calpol Infant 120mg/5ml Sugar Free + Nurofen for Children Strawberry 100mg/5ml — **exactly the two already-confirmed products**, both already matched by `MED_RULES` (`"calpol"`/`"nurofen"`), so no baby-side gap exists (no Paralink etc. in the house). ~~Panadol match-list addition~~ — **shipped v344** (20/07/2026: `"panadol"` added to the calpol rule's `match` array — adults use Panadol Extra Soluble; advisory tracking only, no dose chip, no other wording/formula/interval changes). Back-button phone test DONE 16/07/2026 — confirmed broken; **fix shipped v330** (detailed entry in `HEARTH-archive.md`; the v2 follow-up design is the back-button entry directly below).
- ~~Back button v2 — generic close for the one-off dynamic dialogs + trip-editor back-as-cancel~~ — **shipped v345** (20/07/2026, full detail in the changelog entry above). Manual on-device confirmation of the 8 sheets + the trip/booking editor via the test-mode link is still outstanding — flag for Cathal in the PR.
- ~~Scout finds during the back-button sweep (16/07/2026)~~ — **resolved, shipped v335**: both (a) the dead `#noteEditorOverlay`/`openListNoteEditor`/`closeNoteEditor` and (b) the `renderNotesIn`→`openNoteEditor` signature mismatch turned out to be the same orphaned pre-v318 per-list notes subsystem, confirmed fully unreachable from the UI and removed wholesale (full detail in the v335 changelog entry above).
- ~~Family Activity Log~~ — **shipped v338** (backlogged 06/07/2026; design confirmed 18/07/2026 and **built the same day on Cathal's go-ahead** — full build detail in the changelog entry above; triage notes + confirmed spec kept below as the decision record). Not in v1, each needing its own design confirm before adding: Home/Today-card teaser, search, teeth (no dates stored — would need new logging first). A read-only, chronologically day-grouped "family story" view aggregating significant events from data the app already timestamps: Baby growth/medicine/milestones/teeth, completed Train sessions, trip starts/ends, created notes (`createdAt` exists since the notes rework). Zero new logging, zero new data model, no sync changes. Agreed guardrails from the triage: **hideable section, default hidden** (v308 Sports precedent — no 8th permanent nav icon); significant events only (grocery adds would spam it); ~90-day default window + "load earlier"; medicine rows factual-only ("Calpol 5 ml · 18:40 · check the leaflet") deep-linking into Baby — **all dosing/interval logic stays exclusively in the Baby flow, never duplicated here**; rows deep-link to their source section via existing `switchSection`/modal patterns. v1 scope call from the triage: plain feed + filter chips, **no search** (leaner first cut than the proposal's wireframe). All four open design calls (event list, entry point, filters, layout) were settled with Cathal on 18/07/2026 — see the build spec sub-bullet below. One data-model finding from that chat: **teeth have no dates at all** (`bd.teeth` is a bare `{toothId:true}` map), so teeth are excluded outright rather than deferred — including them would need new logging, breaking the zero-new-data-model guardrail. Reflective nice-to-have, not daily utility — does NOT jump the queue over Cathal-requested items (per-person packing checks, nav consolidation; cold-start flash shipped as v312). Baby-related wording goes through council review per the safety rules.
  - **Confirmed v1 build spec (design chat 18/07/2026; shipped as v338 the same day — kept as the decision record):**
    - **Entry point: own hideable section, default hidden** — replicate the v308 Sports wiring exactly (6 touch points): add `"famlog"` to `HIDEABLE_SECTIONS`, an entry in the `navIds` map, a `bnFamlog` nav button (`display:none` by default, like `bnSports`), a section `<div>`, a `switchSection` branch that calls the new `renderFamlog()` on entry, and a Settings `.section-chk` toggle. Nav label **"Family Log"**, icon 📖. **No Home/Today-card teaser in v1** (explicitly deferred; revisit only if the section proves used).
    - **Event sources (final list) — pure render-time aggregation from existing stores; zero new data model, zero new logging, zero sync changes:**
      - Baby medicine (`fl4_baby` → `.medicine`, fields `{name, dose, ts}`): row like "💊 Calpol · 5 ml · 18:40 · check the leaflet". **Factual only — all dosing/interval logic stays exclusively in the Baby flow, never duplicated here; never drop the "check the leaflet" wording.**
      - Baby growth (`.growth`, `{date, weight, height}`): "📏 Growth: 9.2 kg · 74 cm".
      - Baby milestones (`.milestones`, `{text, date}`): "⭐ <text>".
      - Train completed sessions (`fl4_workouts` → `.workouts`, `{date, sessionName, kcal}`): "💪 <sessionName> · N kcal".
      - Trip boundaries (`fl4_trips`, `{name, start, end}`): "🧳 Trip started: <name>" on `start`, "🏠 Trip ended: <name>" on `end`; skip a boundary whose date is missing.
      - Notes created (`fl4_notes_global` + `fl4_notes_global_work`, `{title, createdAt}`): "📝 Note: <title>" (`createdAt` is reliable — the editor backfills it from `id` on legacy notes).
      - **Excluded, decided — do not re-add without a new design chat:** teeth (no dates, see above); cardio entries (Cathal's call, 18/07); adult bodyweight/BP (personal health telemetry, not family-story material); sports (API fixture cache, not household activity); grocery/list adds (spam).
    - **Layout:** one flat feed, newest first, grouped under date separators (borrow the Trips timeline date-separator pattern); each row = icon + one-line text + time, where the time is shown only when a real timestamp exists (medicine `ts`, note `createdAt`) — date-only entries (growth, milestones, train, trips) show no time. Whole row taps through to its source section via the existing `switchSection` patterns (`baby` / `train` / `trips` / notes → `lists`).
    - **Filters:** chip bar **All / Baby / Train / Trips / Notes** above the feed (same pattern as the Recipes filter bar). **No search in v1.**
    - **Window:** last 90 days by default; a "Load earlier" button at the bottom extends the window by a further 90 days per tap.
    - **Safety:** Baby-related row wording goes through council review before ship, per the safety rules — **not yet run at v338**: the shipped rows use only the pre-agreed factual medicine format; council pass offered in the PR, Cathal's call.
  - **Triage of the other two proposals in the same document (06/07/2026 — do NOT re-chase):** "Hearth Intelligence" daily briefing → **rejected as a feature**: it's the v302 Today card + v306 suggestions + v291 interval advisory re-described; the genuinely new lines are either nags (v302 deliberately chose positive-only), need a nonexistent passport data model, or are surveillance-flavoured pattern-mining ("wife adds yogurt on Fridays"). If a single line ever proves wanted (e.g. "workout due, last done Nd ago") it's a small Today-card addition, not a system. "Hearth Lens" on-device AI vision/OCR → the document itself rejects it and the reasoning is sound (breaks single-file/no-dependency/mid-range-phone constraints); its suggested alternatives are already shipped or backlogged. No action on either.
- ~~Baby growth centile chart — replace the v310 %-of-median chart with true WHO weight-for-length centiles~~ — **shipped v347** (21/07/2026, PR #106, full detail in the changelog entry above). One deviation from the original spec worth recording: `who.int`/`cdc.gov` were both blocked by that session's network policy, so the LMS tables came from a GitHub mirror of the same CDC-redistributed WHO CSVs rather than the primary source directly — the hard "STOP and investigate on any disagreement" rule fired for real (old table vs new data disagreed by up to ~3.5%) and Cathal confirmed proceeding before the build continued. Manual on-device check of the chart + an HSE-book spot-check of a value or two are still outstanding, flagged in the PR.
- **4th external review (received 03/07/2026, reviewed v296 — logged for reference; Cathal flagged it may be stale/lacking full context, and it is).** Its headline recommendation — "make v297 the Train dedupe" — was **already shipped as v297** before this review arrived; likewise it re-flags the field-wise-merge nested-collection limitation that v296 already documents as a known accepted gap. **Nothing new actionable except one item worth noting:** it spotted a **duplicated `setSheet`/`closeSetSheet` + `DOMContentLoaded` + `adj` wiring block** (~line 7592 and ~9545 regions) beyond the 7 functions v297 removed — harmless today (guarded DOMContentLoaded listeners) but the same copy-paste class; **candidate for a small future dedupe pass IF that area is touched.** It also (incorrectly, for a stale copy) claimed the `trainTodayStr` bodies had diverged with extra `getWD`/`saveWD`/`getWorkoutForDate` helpers — v297 verified the 7 pairs were byte-identical before deleting, so treat that as review-of-old-code noise. Everything else in it is already-done, already-documented, or the same correctly-rejected modularisation/IndexedDB suggestions. Its genuinely useful forward ideas (per-entry `updated`+union merge for growth/medicine/bookings arrays; sync-health panel — already Tier 2) are captured below / in the Tier 2 queue.
- ~~Deferred sync hardening — growth & milestones per-entry merge~~ — **shipped v371** (31/07/2026, full detail in the changelog entry above). Design confirmed on Fable 19/07/2026 built exactly as scoped — growth (date-keyed, `updated`+tombstone, the date-change hazard tombstones the old date), milestones (id-keyed, same `mergeMedicine`-style pattern), `mergeBabyData` generalised into shared `mergeIdKeyed`/`mergeDateKeyed` helpers, channel payloads mirror `med_deleted` exactly (`growth_deleted`/`ms_deleted`), backup restore filters both through local tombstones. Nothing left open on this item.
- **v285 council worklist — fully complete** (offline shell v286 · cloud-backup writer v287 + restore UI v288 · dose-interval advisory v291 · housekeeping bundle v289 · Firebase rules verified + account sign-up disabled 02/07/2026 — full detail in the archive entries).
- **Nav consolidation Option B′ — stage 1 ("Track") shipped v339 (19/07/2026); stages 2–4 designed but GATED on a fresh per-stage go-ahead from Cathal.** History: original Option B (Food = Grocery+Recipes, Tasks = General+Personal+Notes, Travel = Trips+Packing) was deferred 09/07/2026 pending a design chat + council. Revisited 19/07/2026 on Fable: Cathal's actual pains (general log + food log buried under Lists→Notes; weight/BP buried under Train) were NOT what original B fixed, so the design became **B′ = the three B merges + a new "Track" section for the three personal logs** (confirmed via 3 AskUserQuestion answers: one section · named "Track" · built first). Council pass same day, verdict **build-with-changes**, conditions all adopted and binding for the remaining stages: **internal section ids NEVER change** (labels/icons/hosting only — `lists`/`recipes`/`trips` stay; `track` was the sole new id), sub-tab hosting extends the `TRACK_VIEWS`-style config table rather than copying patterns, zero data-store moves in every stage, distinct icons (Track 📈 vs Train 💪), **Petra gets a heads-up before the stage that moves Grocery**. **Recorded dissent (Devil's Advocate): stages 2–4 fix nothing Cathal has named — do not build them by default; the backlog item should be CLOSED if no named pain emerges after living with Track.** Remaining stages, one version/PR each, each needing Cathal's explicit go-ahead first:
  - **Stage 2 — Travel (smallest):** keep section id `trips`, relabel section/title "Travel"; host the packing list as an in-section sub-tab named **"Packing"** (it's the Lists sub-tab `data-lt="travel"`, store `fl4_travel` — store and `currentList` key unchanged, container re-hosted the same way Track re-hosted Log/Food/Body); re-point `tripPackingBtn` (currently jumps `switchSection("lists")` + programmatic travel-tab click) to the local tab; v326 per-person checks ride along untouched; remove the Travel tab from the Lists `typeBar`.
  - **Stage 3 — Food (biggest family-facing change — Petra heads-up first):** keep section id `recipes`, relabel "Food"; sub-tab row Grocery | Recipes | Week plan, with the Grocery tab re-hosting the shared list container locked to `currentList="grocery"`; re-point every grocery deep-link that does `switchSection("lists")`+grocery (Today-card suggestion chips etc. — sweep with the scout before building); remove Grocery from the Lists `typeBar`.
  - **Stage 4 — Tasks (rename only):** keep section id `lists`, relabel nav button/title "Tasks"; remaining sub-tabs General | Personal | Notes.
  - **Gate question for stage 2 (ask after ~a week of Track use):** "Now that the logs have their own Track section, do you still want Packing moved into Trips and Grocery in with Recipes — or is the current layout fine?" A "fine as is" answer closes this item per the recorded dissent.
- ~~Add-to-grocery Phase 3 — real-world purchase quantities~~ — **CLOSED 19/07/2026, Cathal's call via AskUserQuestion (Fable design session): the Devil's-Advocate dissent was heeded.** The coverage cliff (a ~15-staple table converts milk convincingly and visibly fails for everything else) plus permanent table upkeep doesn't clear the bar, and the Phase-2 cooking amounts already on grocery items cover the real need. Do NOT re-chase without a fresh Cathal request driven by an actual shopping pain. Decision record in case it is ever revived (both also confirmed 19/07/2026): display would be show-both ("1200ml (≈ 2× 1L)", advisory only, never overwriting the stored amount) with a ~15–20 Irish-staples static in-code table.
- ~~Lists → Track auto-logging~~ — **SHIPPED v357 (25/07/2026), see changelog entry above.**
- **Imported recipes default to "Uncategorised"** — Cathal to categorise over time (the v249 bulk-categorise tool makes this fast: filter to Uncategorised → Select → set).
- Possible backup enhancement (low priority, if ever wanted): a backup reminder independent of app open — the current nudge fires on app open only; daily cloud backup (v287) covers everyday risk.
- Known minor parser gaps: compound amounts like "1 ¾ cups + 2 tbs Greek yogurt" keep the whole string as the name (don't scale → land as name-only on grocery); "Garlic - 2 cloves" (name-first) doesn't split the amount cleanly. Rare; acceptable.
- Open question (low priority): whether to sync the travel tag pool / recipe categories across devices (currently localStorage, so per-device; per-item tags DO sync).

## Shipped (reverse order, condensed — full detail in the version log above for the newest ~10, in `HEARTH-archive.md` for older)
v394 recipe ingredients bulleted + method auto-numbered (display-only; `methodSteps` two-mode splitter, narrow step-marker regex so temperatures/ranges/decimals survive) · v392 Watchlist "Watching" group + optional TV-only season (three states stored as two booleans for v391 mixed-version safety, cycling tick control ○▶✓ plus an explicit status picker, season clamped 1–99, episodes deliberately not built) · v391 Watchlist section (🍿 TV shows + films to watch, household-shared `fl4_watchlist`, whole-record newest-wins like Plants; title-derived JustWatch/Wikipedia/IMDb info links with no API key or stored URL, optional pasted link, TV/Film chips, collapsed Watched group, ★ rating) · v351 dead-code cleanup (duplicated 24-function workout-session region deleted after byte-identity verification, legacy sync-state duplicate vars removed, CI duplicate allowlist emptied) · v350 automated test suite slice 1 (zero-dependency CDP harness, 8 case files, CI wiring + dosing literal pins, mergeHist nameless-entry guard) · v349 chart redesign (one shared visual language for growth/weight/BP trend charts) · v348 grocery autocomplete/suggestion hist-items divergence fix (showAC items fallback + capHistToLiveLimit) · v347 baby growth chart — true WHO weight-for-length centiles via the LMS method, replacing the v310 ratio chart (5-tier centile bands, data cross-checked and Cathal-confirmed after a ~3.5% disagreement with the old approximate table) · v344 add "panadol" to paracetamol advisory match list (confirmed household brand name, advisory-only) · v343 notes sync hardening + work notes sync join (`mergeNotes` id-keyed union, both channels) · v342 fix suggestion chips reappearing after tick-off (done items' `updated` resets the clock + revives feed hist) · v341 weight-diff badge colours inverted (loss = green, gain = red — deliberate, commented) · v340 chart y-axis min/max label overlap fix (value labels to the left margin in all three SVG chart builders) · v339 Track section (nav Option B′ stage 1: Log/Food/Body re-hosted out of Notes/Train into one hideable default-on section + dead duplicate Train block removed, closing council item ④) · v338 Family Log read-only feed section (hideable + default hidden, Baby/Train/Trips/Notes events, filter chips, 90d window + load-earlier) · v337 notes toolbar "?" legend (popover listing every format button's own title attribute, back-button wired) · v336 Body weight/BP charts + Body-first Train sub-tab · v335 remove orphaned pre-v318 per-list notes subsystem (dead `tabNotes`/`notesPanel`/`renderNotesIn`/`openListNoteEditor` + CSS, unreachable since v318) · v333 fix per-person-checks toggle-on revert (Firebase strips empty `checks:{}` off the echo → explicit `{c:false,p:false,b:false}` shape) · v332 confirm dialogs on Clear-completed / Reopen-all · v331 widen `.ppc` per-person circles (council item ③, ~40×48px effective target) · v329 Baby sync hardening (shared per-entry merge on both channels + medicine updated-stamps + med deletion tombstones, council item ②) · v328 note-editor DOMPurify fallback → esc() (security, council item ①) · v327 weather rework (wind + compact layout on card · popup = hourly +wind + 3 upcoming days) · v326 per-person travel packing checks · v325 "Recent" 48h filter chip on all lists (+ per-item `added` stamp, revive paths now stamp `updated`) · v324 pre-merge test links on every PR + in-app test mode (off-domain = banner, Firebase/login hard-blocked, no SW) · v323 history per-item + multi-select delete (synced name-keyed tombstones) + grocery Clear-completed removed · v322 home live clock + back-button nav · v321 fix trip deep-link id type · v320 Today-card trip deep-link · v319 Sports Today-card entry point · v318 nav consolidation Option A (bar 7→5: Notes into Lists · Train off-bar by default · Trips packing link) · v317 Week plan dinner planner (7-day tap-to-assign + add-week-to-groceries, household-synced) · v316 weather card nearby-town name (offline Irish town table) · v315 Recipes opens on Favourites · v314 header badge leak fix · v313 weather card H/L–Rain% swap · v312 cold-start boot splash + version gate · v311 Sports recursion crash fix · v310 growth chart redesign (single WHO-ratio chart) · v309 visual growth chart · v308 sports scoreboard section · v307 suggestion-chip tap targets · v306 predictive grocery suggestions UI · v305 rain % on weather card · v304 voice capture (speak-a-list) · v303 replenishment data foundation (invisible) · v302 Today summary card · v301 growth-trend summary · v300 autofill/password-prompt fix · v299 sync-health panel · v298 trips departure badge · v297 train dedupe · v296 field-wise baby/trip merges · v295 SW lie-fi fix + tombstones 90d · v294 cruft removal (baby list + test lists) · v293 flight seats field · v292 trips boarding/gate + one-step connections · v291 dose-interval advisory + weight-date · v290 restore-dialog z-order + boot watchdog · v289 housekeeping (DOMPurify vendored · tombstones 30d · recipe stamps · touch targets) · v288 cloud restore + import gaps (trips/notes/tombstones) · v287 daily cloud backup (keep-7) + nudge 30d · v286 offline app-shell fix (network-first + cache fallback) · v285 trips scroll restore · v284 trip expand/collapse all · v283 trip 24hr times + maps link · v282 trip timeline polish · v281 trip import merge into existing · v280 trip JSON export + import · v279 fix timeline same-day ghost cards · v278 timeline view + date separators · v277 connecting flight legs + transfer time · v276 fix booking editor on sync · v275 Trips section · v274 location-based weather · v273 hourly weather strip · v272 manual grocery amount editing · v271 fix dashboard duplicate add · v270 scroll-to-top everywhere · v269 recipe search + scroll restore · v268 recipe deletion sync · v267 feels-like on weather card · v266 version label date-only · v265 timestamp diagnostic bump · v264 weight-based medicine dosing · v263 baby medicine quick-chips + babySex-save fix · v262 weather 3-day sheet · v261 home layout cleanup · v260 front-page ★ Today · v259 weather timestamp · v258 weather card · v257 tap-zone fix · v256 "Today" picks · v255 partner-recipe toast · v254 noindex · v251–253 backup nudge · v250 method formatting · v249 bulk categorise · v248 recipe-editor-no-close · v247 list convergence re-push · v246 grace window removed · v245 union/newest-wins/tombstones · v243 sync send-wedge fix · v242 newest-wins recipe sync · v241 grace catch-up · v240 recipe sync · v237–239 add-to-grocery (names→amounts→headers) · v236 recipe categories+favourites · v234 CSV import · v232 Recipes section.

## Open decisions on record
- **Merge tie-break asymmetry — left as-is, deliberately (25/07/2026, from the v351 bug review):** `mergeListItems` gives same-stamp ties to the REMOTE copy; `mergeNotes` and `mergeTripsData` give ties to LOCAL. The asymmetry is historical rather than designed, but it stays: once every mutation site stamps `updated` (fix-queue batch B) the tie-break almost never decides anything, and flipping either direction is a sync behaviour change with mixed-version risk for no user-visible gain. Do not "harmonise" it; do keep new merge helpers explicit about which way their tie goes.
- **Note star = a modification (25/07/2026):** starring/unstarring a note bumps `updatedAt`, so the editor's "Modified" date jumps on a star. Accepted trade-off — the alternative (a separate star stamp + a rule in `notesTs`) was rejected as machinery. Means the star syncs like any edit.
- **Backup restore semantics for bodyweight/BP (25/07/2026, fix-queue batch A):** additive union, keep-local on a same-date/same-ts collision — consistent with the rest of `importBackupData` being additive-only. Backup-wins rejected.
- **Growth date-edit collision (25/07/2026, fix-queue batch D):** refuse with a toast, never overwrite or keep-both — growth entries feed the WHO centile chart and the dosing weight, so silently discarding or duplicating a measurement is the worst outcome.
- **Week plan (v317):** household-shared chosen over device-local (it's coordination data, like grocery/recipes); dinner-only rolling next-7-days (no week navigation, no lunch/breakfast slots in v1); lives inside Recipes (no new nav icon, deliberate given pending nav consolidation); per-recipe "Add to grocery list" kept unchanged alongside it. Merge keyed on the day string (newest-wins) chosen over ids+tombstones so unassignment needs no deletion machinery. Drag-and-drop, nutrition APIs and recipe photos rejected (same fiddly-touch / third-party-dependency grounds as prior decisions).
- Add-to-grocery: **Option B** chosen (push items to Grocery **and** save a reusable chip in `fl4_recipes`). Saved chip stored at **base servings**. Re-tapping **updates the chip quietly + always re-adds the tagged items**.
- Add-to-grocery amounts (Phase 2): **Path B** chosen — amount is a **separate `item.amount` field**, not baked into the name (keeps name as dedupe key, avoids freeform parsing, and gives Phase 3 something to build on). Scale at **current stepper servings**; **new items get amounts, existing items re-tagged only** (never overwrite a hand-typed quantity). Base-vs-current divergence is deliberate: chip = base, grocery labels = current.
- Play Store + push notifications: rejected as disproportionate; app stays personal/family.
- Direct Claude↔GitHub editing: rejected to preserve the manual review checkpoint.
- Recipe sharing: started as **merge-by-id / incoming-wins** (v240), which clobbered edits when a stale device pushed. Replaced in **v242** with **newest-wins by `updated` timestamp + convergence re-push** on both the household and personal channels. Accepted costs: **deletions don't propagate**, and concurrent edits to the same recipe resolve as newest-edit-wins (older lost). Recipes are shared whenever a household is set (no per-section toggle).
- List sync model (v245): **union-by-id + per-item `updated` newest-wins + deletion tombstones**, applied identically on the household and personal channels (replaced the old household wholesale-replace, which lost concurrent adds). Tombstones (`fl4_tomb_<lt>`) are transient sync metadata, 90-day purge (7d→30d v289, →90d v295), deliberately excluded from backup/export.
- Recipe stores kept separate: `fl4_recipebook` (Recipes section) vs `fl4_recipes` (grocery quick-import).
- **"Today" view (v256): Option B** chosen — a manual **pick-for-today focus view**, not a recurring/daily-reset flag. **Persistent (no auto-reset)** so it adds zero new sync machinery (rides the per-item `updated` merge); surfaced as a **filter chip on General**, not a separate tab; **completing an item clears its `today` flag**; scoped to General only. The recurring/auto-clear alternative was rejected to avoid day-rollover + double-reset sync hazards.
- **Accidental-tap fix (v257): neutral-buffer approach** chosen — notes open only on the item body text, with a dead-zone gap beside the checkbox. **Long-press-to-open-notes was rejected** as too fiddly on touch (scroll-cancel, click-suppression, native context-menu collision, weak discoverability). Applies to all detail lists, not just General.
- **Weather location (v274): auto-detect with fallback** chosen — `navigator.geolocation` on every weather fetch, silently falling back to the hardcoded Carrigaline coords (v258) on denial/timeout/no support. **No manual Settings toggle** (rejected as one more thing to remember before/after travel). **Generic "Current location" label** chosen over an extra reverse-geocoding call to show the actual city name (rejected as an unnecessary third-party dependency for a "nice to have").
- **Trips section (v275):** named **"Trips"**, not "Travel" — the existing Lists→Travel packing list already owns that name. **Booking-level sync granularity** chosen over whole-trip newest-wins (a trip is a *container* of independently-editable bookings, so whole-trip replace risked clobbering a partner's edit to a different booking in the same trip — unlike a flat recipe). **Hideable nav entry** (added to `HIDEABLE_SECTIONS`, like Train/Notes/Home) rather than permanently pinned. Nav position: **end of the bottom bar** (confirmed via direct question, options were "right after Recipes" vs "at the very end"). Each leg of a multi-leg journey is its **own separate booking card** (no combined round-trip object). Deferred to a later version: countdown to departure, link to the Travel packing list, per-traveller passport info, budget.


---
---

# Hearth — Architecture & Working Notes

A reference for any new chat working on the Hearth PWA. Read this first.

## What Hearth is
- A family-hub Progressive Web App for two people (Cathal + partner Petra).
- **One single-file vanilla HTML/JS app**: `index.html` (~12,100 lines, ~715k at v326) plus `sw.js` (service worker).
- No build step, no framework. Plain HTML/CSS/JS.
- **Hosting:** GitHub Pages at `coshea321.github.io/miniature-journey`
- **Storage:** localStorage (primary) + Firebase Realtime Database (household sync between the two users)
- **Users:** `cathal1` (Cathal) and `petra`. Items can carry `addedBy` and sync between devices.

## Who maintains it & how
- Cathal is a **non-coder**. He reviews and merges PRs; Claude Code does all the coding.
- Claude Code (CLI / web) works on the file directly, commits and pushes to a feature branch, and **opens a PR immediately** — Cathal reviews the diff on GitHub and merges to `main`. The human-review checkpoint is deliberate; Claude Code never pushes directly to `main`.
- Before each change, Claude Code creates a `backup-vNNN` branch (frozen snapshot of the prior version) for rollback — the `hearth-preflight` subagent does this. Retention: keep the newest 10; older ones are named for Cathal to delete on GitHub (branch deletion is blocked from remote sessions).

## Sections (bottom nav — 5 icons by default since v318, plus Track (v339) and Watch (v391))
Home · Lists · Recipes · Baby · Trips · Track · Watch (+ Train, Sports, Family Log and Plants as opt-in bar icons via Settings; Notes is a sub-tab inside Lists; Train is always reachable from the Home Training card)

- **Home:** order is greeting (+ live clock, v322 — a second "Home (Ireland) HH:MM" line appears only when it differs from local time, i.e. while travelling) → weather → **Today summary card** (v302: self-suppressing tappable one-liners — 🛒 groceries to buy · ⭐ today's tasks · 💪 workouts done today · 👶 last medicine if logged today · ✈️ next-trip countdown, deep-linking to the named trip since v320/v321 · ⚽ next cached fixture since v319; "✨ All caught up for today" when every line is empty) → Training strip → Baby strip → **Plants card** (v379: always shown — count + "N need watering" / "All watered · next in N days"; the permanent door into a section with no nav slot) → quick-add → ★ Today → Grocery → week strip (v261 base order). **Weather card** under the greeting (v258): current temp + condition + humidity + high/low for Carrigaline, via Open-Meteo (no key); device-local, cached in `hearthWeather`, refetched only if >30 min stale; hidden if no data and offline; "as of HH:MM" timestamp always shown (v259); **tap opens a 3-day detail sheet** (v262: feels-like, humidity, per-day high/low + 💧 rain chance + 💨 wind), **plus an hourly scroll strip for the next 24h** (v273: icon/temp/rain% per hour); **feels-like shown on card** (v267); **follows device GPS with Carrigaline fallback** (v274); the card label shows the **nearest Irish town name** via an offline ~37-town Haversine table (v316), falling back to "Current location" (abroad / away from listed towns) or "Carrigaline" (no GPS); **☔ rain % on the card** (v305; meta order Feels → H/L → Rain since v313). **Training strip** (v261): compact full-width horizontal card (sessions today + kcal), taps through to Train. **Baby strip:** last medicine + latest weight. **"★ Today" to-do spotlight** (v260, moved above Grocery in v261): only tasks starred for today (v256 `today` flag), empty state "🎉 No tasks for today". The old "active items" tile was removed in v261. Grocery spotlight unchanged.
- **Lists:** Grocery, General (todo), Personal, Travel. Collapsible categories, per-item notes/links/due dates, search (fuzzy, toggleable), print, import, duplicate highlighting, partner-added "NEW" indicators, expand/collapse all. Travel has multi-tag items + a tag filter bar (incl. "Untagged"). Grocery has recipe-tag import (tag a batch, clear/revert as a group) with reusable saved recipes. **General has a "Today" pick** (v256): a ☆/★ star per item sets a manual `today` boolean, with a **★ Today (N)** filter chip at the top; ticking an item done clears its today flag; it syncs via the normal per-item `updated` merge (no separate machinery). **Tap zones on item cards** (v257): the circle checks off; the **item body text (`.item-body`) opens the notes sheet**; the gap between them is a deliberate neutral buffer (does nothing) so a near-miss on the checkbox doesn't open notes. Category pill, ★ star, and ✕ delete are their own zones.
- **Train:** yoga flows + physio programmes. Every exercise timed/auto-advancing; one-sided exercises are explicit (Right)/(Left) entries each with their own chime. tsp/tbsp-style timing rules don't apply here.
- **Recipes:** recipe book with ingredients/method/servings/notes, servings scaler, categories + favourites, CSV batch import.
- **Baby:** growth (WHO weight-for-length), medicine, milestones, teeth, packing bags, sex selector.
- **Plants (v377, opt-in icon):** houseplant care book. List → detail: photo/name/botanical name, a watering + feeding panel (last done, next due, one-tap Log with same-day Undo), then the care write-up — summary, watering, feeding above a "More on this plant" divider, with light, repotting, propagation, safety and notes below it. Sections come from the top-level `PLANT_SECTIONS` table; empty ones don't render. Photos are shrunk to a ~400px thumbnail before storage. Overdue plants surface on the Home Today card. **Home card (v379):** an always-present 🪴 Plants card in the Home glance grid — count + one status line, tapping it opens the unfiltered list; this is the permanent way in, since Plants has no nav-bar slot by default. **Area filtering (v379):** the `room` field doubles as the area (labelled "Area" in the editor, with a datalist of the areas already in use); a chip row above the list filters All · each area · No area, appearing only when there are at least two options, and the overdue banner counts whatever the filter is showing. The filter is session-only and never stored. **Import/export (v378):** 📥 in the header opens a two-step importer (paste a `plant-v1` file → add as new, or update an existing plant) with a copyable AI prompt template built from `PLANT_SECTIONS`; export buttons sit on the detail view (one plant) and under the list (all plants, written care notes only — photos and logs stay in the Settings backup).

## CRITICAL technical quirks (these cause bugs if ignored)

1. **Duplicate function definitions (the old "38 duplicates" incident — deduped in v297; verify counts before editing).** Many Train/body/history functions once existed as TWO top-level copies, where the second silently won via hoisting — editing the first copy was a silent no-op. v297 deleted the last seven duplicate pairs (`trainTodayStr`, `trainShiftDay`, `trainFmtDate`, `trainFmtDateLong`, `timerCircle`, `wireInputs`, `wireWorkoutLog`), v339 removed the duplicated `openSetSheet`/`closeSetSheet` + `DOMContentLoaded` block, and **v351 removed the last duplicated region** (the 24-name workout-session block the v350 CI grep found, plus the duplicate `syncPaused`/`lastPushTs`/`syncPrefs` declarations) — **as of v351 every top-level function and var has exactly ONE definition, and the CI duplicate-grep allowlist is empty, so any new duplicate fails the PR checks.** Standing lesson stands: grep a function's definition count before editing it.

2. **Two real `<script>` blocks — syntax-check the RIGHT boundary.** The file has one external script (DOMPurify, self-closed) then the **main inline app block**; a third `<script>` appears only *inside a JS string* (the print-doc) and is not a real block. To check the main app code, extract from the `// PREVIEW MODE` comment to the **FIRST** `</script>` after it, then `node --check`. **Using the LAST `</script>` produces a convincing FALSE error** — it sweeps up the print-doc HTML string and other script blocks, and `node --check` reports a syntax error (often "Unexpected token '<'") that is NOT real. If a syntax error appears, first confirm you used the first-`</script>` boundary before chasing it. **Also: never hard-code the boundary line number from a previous version — every edit shifts line numbers, so a stale `sed -n 'A,Bp'` range will truncate the script mid-code and throw a false "Unexpected end of input". Re-derive the first-`</script>` line dynamically each time (e.g. `awk 'NR>=START && /<\/script>/{print NR; exit}'`) before extracting.**

3. **Python `str_replace` fails silently.** Always grep-verify a replacement actually landed (count occurrences before/after).

4. **Never write at a computed index without checking `find()` succeeded.** The v221 corruption came from a Python edit where `find()` returned −1 and the code then wrote at `idx + len(...)`, which appended a FULL DUPLICATE of the document after `</html>` — doubling the file to ~923k chars (two "Hearth" headers, raw `no-cache` meta visible as text, broken nav). If a find/anchor might miss, assert it found something before slicing/inserting.

5. **End-of-file edits are dangerous — verify structure after.** After ANY edit near the end of the file, verify: total length is sane (**~557k at v267, ~665k at v308, ~715k at v326**, growing slowly each version — a sudden jump to ~900k+ means the v221-style doubling), exactly ONE real `<html lang>`, ONE version string, DOCTYPE count of 2 (one real + one inside the print-doc JS string). The fix for the v221 doubling was truncating at the first `</body>\n</html>`.

6. **Scope: helpers called from top-level renderers must be top-level.** `renderList`, `openItemSheet`, `switchSection` etc. are top-level functions. A helper they call (e.g. `getTravelTags`, `renderItemSheetTags`) that is accidentally defined INSIDE a setup/IIFE block throws "X is not defined" the moment the renderer runs (this was the v226 bug). Define such helpers at top level.

7. **Bottom-of-page sheet wiring must run after DOM-ready or be null-guarded.** Several sheets (import, recipe import, item sheet) have their HTML near `</body>`, AFTER the main script. Code that calls `getElementById(...).addEventListener` on those elements at top-level script time hits `null` (the v235 crash). Either defer wiring with `DOMContentLoaded` (with an `else wire()` fallback if already loaded) or null-check every element before wiring.

8. **`confirmDialog` is OK/Cancel only.** It does NOT take custom button labels or a third option. When a 3-way choice is needed (e.g. recipe clear: delete added / keep added / cancel), hand-build a small overlay dialog — don't assume confirmDialog can do it.

9. **Check whether a feature already exists before building it.** This codebase has had features added across many sessions; some are present but not in any summary. Twice, work was started on something already built (list search; a collapse-arrow CSS rule). grep for the feature's likely identifiers first.

10. **`var history` conflicts with `window.history`.** Declaring `var history = []` (or any value) in browser global scope silently fails in some Android Chrome versions — `window.history` is a protected browser object and the assignment is ignored, so `history.sort()` later operates on the real `History` API and blows up. **Never use these names as global variables:** `history`, `location`, `name`, `status`, `frames`, `top`, `parent`, `self`. Use `hist`, `loc`, `roomName`, etc. instead. (Discovered building FreshList v3-v4; caused items to disappear after first add.)

11. **Firebase SSE self-echo race condition.** Every local push to Firebase triggers an SSE event back to the same device carrying the pre-push state. If that event is applied naively it overwrites locally-added items — the "items disappear after 8 additions" bug. The v245/v246 Hearth solution (union-merge + newest-wins + tombstones, apply immediately) already handles this correctly. If ever revisiting sync: **do not re-introduce a blanket grace-window that drops all SSE events** — it silently kills incoming changes from other devices. The right model is merge-then-re-push-if-fuller, not drop.

12. **Format toolbar on mobile goes at the bottom.** When building any rich-text editor with a toolbar, put the toolbar at the **bottom** of the screen. Android's native text-selection popup (Copy/Cut/Paste) appears near the selected text, which is usually in the upper part of the editor — directly over a top toolbar. Bottom toolbar avoids the collision entirely. Use `padding-bottom: max(10px, env(safe-area-inset-bottom))` to respect the home indicator on notch devices.

13. **PWA manifest icons must be real PNG files with separate `any` and `maskable` entries.** Using SVG data-URIs in manifest `icons` or combining `"purpose": "any maskable"` into a single entry prevents Chrome from showing the install prompt. Always generate actual `.png` files and list them as two separate objects:
    ```json
    {"src":"icon-192.png","sizes":"192x192","type":"image/png","purpose":"any"},
    {"src":"icon-192.png","sizes":"192x192","type":"image/png","purpose":"maskable"}
    ```

## Data & export
- Export auto-includes anything nested in `getWD()` (workouts incl. bodyweight/bp), `getBD()` (baby incl. growth/medicine/milestones/teeth/bags/babySex), `getActionLog()` (incl. cardio), plus list items themselves (incl. per-item `amount`, `tags`, `today`, `recipe`, `added` (v325), `checks` (v326) fields).
- Top-level stores also exported: `food_log`, `food_notes`, `saved_meals`, `cal_goal`, `secVisible`, `syncPrefs`, `recipes` (grocery import recipes), `travel_tags`, `recipebook` (Recipes section), `mealplan` (week dinner planner, v317), `watchlist` (Watchlist section, v391 — the watching/watched status, the season (v392), the ★ rating and any pasted link all ride inside the records; the three info links are built from the title at render time and are deliberately never stored), `plants` (Plants section, v377 — the photo thumbnail and the watering/feeding logs ride inside the plant records, so one key covers them).
- A coverage-checklist comment sits above `exportData`. **Only touch export if a brand-new top-level `storeSet` key is added** outside the nested structures above.
- **Deliberately NOT exported/synced** (transient or device-local): `fl4_tomb_*` (deletion tombstones incl. the `fl4_tomb_hist_<lt>` history tombstones (v323) and `fl4_tomb_med` medicine tombstones (v329), 90-day purge since v295), `hearthWeather` (weather cache), `fl4_sports_teams` / `fl4_sports_cache` (Sports config + fixture cache, v308), `fl4_sugg_snooze` (grocery-suggestion snoozes, v306), `fl4_trainNavMigrated` (one-off v318 boot-migration flag), `fl4_last_backup` / `fl4_backup_snooze` (backup-nudge timing), `fl4_last_cloudbackup` (daily cloud-backup stamp), `fl4_catOpen_*` / `fl4_doneOpen_*` (UI state), `fl4_seen_*` / `fl4_unseen_*` (partner-item tracking).
- **Daily cloud backup (v287):** the same `buildExportPayload()` output is PUT to `/hearth/users/<user>/backups/<YYYY-MM-DD>` once per day per device, newest 7 kept. The payload builder is shared with the file export — when adding a new top-level store, one edit covers both.
- **`_recipePrev` / `_recipeAdded` / `_recipeNote` (recipe-tag bookkeeping fields on grocery items) DELIBERATELY ride along** into localStorage, both sync payloads and the backup file (documented 25/07/2026, v351 bug-review finding 13; `_recipeNote` added v364 — it holds the exact note line the recipe wrote, which is the only way to find that line again once the "… for &lt;recipe&gt;" suffix was dropped): the recipe clear/revert flow needs them to survive a reload, so they are not transient in the `fl4_tomb_*` sense. Don't strip them from export/sync, and don't move them to a side store without a design chat.

## Key storage keys
`fl4_workouts`, `fl4_baby`, `fl4_action_log`, `fl4_food_log`, `fl4_plants` (Plants section, v377 — **shared household-wide**, whole-record newest-wins by `updated`; `fl4_tomb_plants` holds its deletion tombstones, transient/90d/NOT exported like every other `fl4_tomb_*`), `fl4_watchlist` (Watchlist section, v391 — **shared household-wide**, whole-record newest-wins by `updated`, same contract as `fl4_plants`; `fl4_tomb_watchlist` holds its tombstones, same transient/90d/not-exported policy), `fl4_recipebook` (Recipes section — **shared household-wide** via newest-wins merge), `fl4_mealplan` (week dinner planner — **shared household-wide**, newest-wins per day, self-pruning >14d), `fl4_recipes` (grocery import recipes), `fl4_travel_tags`, `fl4_tomb_<lt>` (list deletion tombstones, transient, 90-day purge), `fl4_tomb_med` (medicine deletion tombstones, v329 — same transient/90d/not-exported policy), `fl4_catOpen_*` / `fl4_doneOpen_*` (UI state, NOT exported), `fl4_seen_*` / `fl4_unseen_*` (partner-item tracking, NOT exported), `fl4_secVisible`, `fl4_syncPrefs`, `fl4_last_backup` / `fl4_backup_snooze` (backup nudge, transient), `fl4_last_cloudbackup` (cloud-backup stamp, transient), `hearthWeather` (weather cache — current + 3-day forecast; device-local, NOT exported, NOT synced), `fl4_sports_teams` / `fl4_sports_cache` (Sports teams + fixture cache — device-local, NOT synced/exported), `fl4_sugg_snooze` (grocery-suggestion snoozes — transient, NOT exported), `fl4_trainNavMigrated` (one-off v318 boot-migration flag).

## Recipe unit rules (Recipes section)
- tsp and tbsp **stay** as tsp/tbsp at any serving size (do not convert to ml).
- ml stays ml.
- cups convert to **grams** for dry/weighable ingredients (via a name-matched density table) or **ml** for liquids (1 cup ≈ 240 ml).
- Counts (cloves, cans, slices, pieces, cubes, pinches) scale via `fmtNumber` — **not** rounded to whole numbers (e.g. 3 cloves × 0.5 → "1 ½ cloves", corrected 25/07/2026 per the automated-tests review D4) — and pluralise on any value other than exactly 1.

## Standard per-change workflow (delegated to subagents since 05/07/2026)
1. **Run the `hearth-preflight` subagent** before any edits — every time, without exception. It fetches and confirms the base is the latest `origin/main`, creates and pushes the `backup-vNNN` branch (current version, before any edits; **retention: keep the newest 10** — deletion is blocked from remote sessions (403), so it names the ones outside the window for Cathal to delete on GitHub, repo → Branches → 🗑️), reports version state across sw.js/index.html/notes, and greps for already-built features when given keywords. Resolve any flags before editing. Never push to or delete `main`.
2. Edit incrementally — never rewrite the whole file.
3. **Run `node tests/run.js` directly (since v350, cheap — don't spawn a subagent for it)**, then the **`hearth-verifier` subagent**, after edits and before committing. The verifier grep-verifies edits landed, `node --check`s the extracted main script block (PREVIEW MODE → first `</script>` AFTER it), checks file length / DOCTYPE count (2: one real + one in the print-doc string) / single version label / clean EOF, version-bump consistency, and (check 6) re-runs the test suite itself. A DO NOT COMMIT verdict means stop and fix. (If subagents are unavailable, the full manual checklists live in `.claude/agents/hearth-preflight.md` and `.claude/agents/hearth-verifier.md` — run them by hand.)
4. Bump the version string in BOTH `index.html` and `sw.js`. Format: **`vNNN · DD/MM/YYYY`** (date only — no `HH:MM`). The displayed bottom-of-page label is sourced from the `sw.js` `VERSION` constant at runtime (the HTML label is overwritten via the `SW_VERSION` postMessage), so `sw.js` is the source of truth for what the user sees; a stale-looking version means the new SW hasn't activated yet, not a bug.
5. **Update `HEARTH-notes.md`**: add a changelog entry (keep "Recently completed" to the newest ~10 detailed entries — move the surplus oldest one to `HEARTH-archive.md`), update the Current version line, and sync the backlog.
6. **Commit, push, and immediately open a GitHub PR** — always, without waiting to be asked. Cathal merges to `main` from the PR; never say "create a PR when you're ready". **The PR body must START with the "👉 Try this version" test link** (v324): `https://raw.githack.com/coshea321/miniature-journey/<branch>/index.html` — serves the branch with in-app test mode active (orange banner, sync/login hard-blocked, sandboxed storage). **One PR per version (02/07/2026):** Cathal merges within minutes of a PR opening, so pushing a second version to the same branch strands it on a merged PR (happened with v287 on PR #25 → rescued as #26). Fetch/rebase onto latest `main` before starting each version; every version gets a fresh PR. The `/release` command (`.claude/commands/release.md`) automates steps 4–6 plus the verifier run. A GitHub Action (`.github/workflows/hearth-pr-checks.yml`) re-runs the mechanical checks on every PR as a backstop.

## Tests (since v350)
Zero-dependency behaviour test suite in `tests/` (spec: `HEARTH-tests-review.md`) — no Playwright, no npm, no `node_modules`. `tests/harness.js` drives the pre-installed headless Chromium over raw CDP (Node ≥22 built-in `fetch`/`WebSocket`); `tests/run.js` boots `index.html` from `file://` and runs every file in `tests/cases/` (currently 8: boot smoke, the 4 sync merge functions, trip round-trip, backup round-trip, dose e2e), asserting zero page errors throughout.
- **Run before committing:** `node tests/run.js` — expect exit 0. Cheap (~15–20s), part of the standard per-change workflow now (step 3, alongside the verifier).
- **CI:** `.github/workflows/hearth-pr-checks.yml` runs the same command on every PR, plus dosing literal-pin greps and a duplicate-function-name grep (allowlist emptied in v351 — any duplicate fails). Currently **report-only** (D3) — doesn't block merging until Cathal adds branch protection.
- **Editing a test IS an app change:** if a PR touches anything under `tests/`, say so explicitly in the PR body ("tests changed: which file, why") — a behaviour change must never slip through disguised as a quiet test edit. Cathal reviews test diffs like app diffs.
- **On a red ✗:** don't merge. The Checks tab shows `::error::` lines in plain English (which case, what broke) — paste them to the next Claude session, or comment on the PR if one is watching it.
- Slice 2 (parked): grocery-suggestions + centile-golden-value cases, same harness, `09-`/`10-` case files.

## Device/workflow notes
- **Active workflow:** Claude Code commits + pushes to a feature branch; Cathal reviews the PR diff on GitHub and merges to `main`. GitHub Pages serves the updated app within seconds of merge.
- **Pre-merge testing (v324):** every PR body starts with a "👉 Try this version" raw.githack link serving the branch with test mode active (orange banner, sync/login hard-blocked, sandboxed storage). Cathal taps it on his phone, pokes the change, then merges. For realistic data: export a backup from the real app, import it on the test page — nothing flows back.
- Backup branches (`backup-vNNN`) are the per-release restore points — frozen snapshots pushed to GitHub before every change.
- GitHub edits on mobile (if needed without Claude Code): use **Chrome with "Desktop site"** — the GitHub app can't edit files.
- The old "download zip → extract on Redmi → re-upload to GitHub" flow is superseded by the git/PR workflow. It remains a valid manual fallback if Claude Code is unavailable.

## Review council (on request)
When Cathal says "use my council" / "council review", invoke the **`hearth-council` skill** (`.claude/skills/hearth-council/SKILL.md`) — the full 7-expert spec lives there and loads only when invoked.

## Sync troubleshooting
- **A device receives but can't send.** Most likely cause is NOT a code bug. On Petra's tablet (during v255) the root cause was **low device storage evicting the saved login token / household code** from localStorage — so the device could read the shared doc but had no credentials to push. The tell is a network/auth error on login or a silent failure to send. **Fix: free up storage, then re-login** to restore the token + household code. (If it were code, the v243 `try/finally` guard would have surfaced "applyHousehold error (sync continues)" in the console — check there first to rule code in or out.)
- **Changes apply slowly / only after navigating away.** Was the pre-v246 grace-window symptom; removed in v246. If it recurs, do NOT reintroduce a blanket SSE grace window (see lesson #11) — the correct model is merge-then-re-push-if-fuller.


---

## Cross-app lessons (from My Health session, June 2026)

Lessons from a parallel vanilla React PWA session that apply equally to Hearth:

14. **`<\/script>` with a literal backslash causes a blank page with no JS errors.** Chrome's HTML parser looks for `</script` (case-insensitive) to close a script element. `<\/script>` — with a backslash — does NOT match, so external `<script src="…">` tags appear unclosed and the main inline script block never executes. The page renders blank and the console shows nothing from the app (only extension noise). This creeps in from editors or copy-paste that escape closing tags for use inside JS strings. Fix: `sed -i 's/<\\\/script>/<\/script>/g'` or a plain find/replace in the GitHub editor.

15. **Edge Tracking Prevention blocks unpkg.com/CDN scripts on `file://` URLs.** Microsoft Edge treats external CDN requests (React, etc.) as third-party trackers when the page is served from `file://`. Result: React never loads, page is blank. The tell is `"Tracking Prevention blocked access to storage for <URL>"` in the console — NOT a JS error. Fix: use Chrome for local PWA development, or serve via `npx serve .` on localhost.

16. **Browser extension errors in the console are not app errors.** The Apollo Client DevTools extension logs `content.js: ApolloClient connectToDevTools` on every page load regardless of what the page does. It is completely benign. In general, if an error references `content.js` and a line in the hundreds/thousands, it is an extension content script, not app code.

17. **Image-to-named-item mapping must always be user-verified.** Never guess photo→pose (or photo→anything) mappings from album order, filenames, or sequence. Always ask the user to open each URL and read the label/content directly. Wrong mappings are completely invisible at code time and only show up visually in the app.

18. **bash tools can go unavailable mid-session.** When `bash_tool` and `str_replace` become temporarily unavailable, present pending changes as clearly-labelled code artifacts with exact find/replace strings so the user can apply them manually or carry them to the next session. Never silently drop changes that were discussed but couldn't be applied.

19. **Per-leg/per-side timer pattern.** For timed physio/exercise exercises requiring separate timers per leg or side: add `sets:2, perLeg:true` to the exercise data object; in the UI, check `ex.perLeg` to show `"Right leg"` / `"Left leg"` instead of `"Set 1 of 2"` / `"Set 2 of 2"`. All other exercises with `perLeg` absent fall back to the normal set label automatically.

---
---

## sw.js
`sw.js` is in the repo root alongside `index.html`. Read it directly — no snapshot needed here.

It is a **single-source-of-truth file**: changing the one `VERSION` constant updates the cache name and the SW version message together, so a routine version bump is a **one-line edit**. The version format is `vNNN · DD/MM/YYYY`.
