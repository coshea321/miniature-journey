# Hearth — Notes & State of Play (as of v323) 

**This is the single source of truth for the Hearth project.** It combines: state-of-play (version log + what's pending), architecture & working notes, and hard-won lessons. The only other context doc is `HEARTH-archive.md` (detailed changelog entries older than the newest ~10 versions, split out 05/07/2026) — **read it only when an old version is referenced**, not at session start. If an older separate `HEARTH-architecture.md` or `HEARTH-state-of-play.md` is still in Project knowledge, delete it; this file supersedes both.

**This file lives in the repo** (`HEARTH-notes.md` at root) and is updated by Claude Code at every release. A new chat session can read it directly — no upload needed. To get a fresh session up to speed: open Claude Code on the web, point it at this repo, and it reads `CLAUDE.md` + `HEARTH-notes.md` automatically.

## ⚙️ Maintenance habit (keep this note)
**Update `HEARTH-notes.md` in the repo at every release** — changelog entry, version bump in the sw.js block, and backlog sync. Since the file is now in the repo, Claude Code reads it directly at the start of each session; there is no need to re-upload it to Project knowledge. The old "download and re-upload" flow is obsolete.

## Current version
**v323 · 11/07/2026** — last shipped build (version label is **date-only**, sourced from `sw.js` VERSION constant).

## External review triage (02/07/2026 — Cathal supplied 3 independent app reviews; full plan agreed)
Claims verified against code before acting. **False/already-handled (do NOT re-chase):** "apostrophe DOM crasher" — `esc()` already escapes `'`→`&#39;`; "SW cache grows forever" — old caches are deleted on every activation (cache name = version); "version maintained in multiple places" — label is already runtime-sourced from sw.js, the dual bump is deliberate; "localStorage quota unhandled" — `storeSet` already catches QuotaExceededError with a toast; "38 duplicated Train functions" — stale count from an old incident, **7 real duplicates remain** (trainTodayStr, trainShiftDay, trainFmtDate, trainFmtDateLong, timerCircle, wireInputs, wireWorkoutLog — second definition silently wins; dedupe = v297). **Real and accepted:** SW "lie-fi" hang (fixed v295); Baby object rebuilt from a hand-typed field list in sync-apply → future fields silently wiped (babySex incident class; fix = v296 field-wise merge, same for bookings); tombstone window (90d in v295). **Rejected on philosophy/churn (all three reviews pushed these):** splitting the file into modules, IndexedDB migration, incremental renderers, execCommand replacement, desktop cosmetics — single-file + guardrail workflow stands; CSS extraction is the safe first slice IF maintainability pain ever becomes real. **Tier 2 queue (agreed, each needs a design confirm):** Trips "next departure in X days" badge · "Today" aggregator card on Home · Baby percentile trend (WHO data already present) · sync-health panel in Settings. Parked: recipe-parser second pass, dashboard customisation, recurring items, global search, polish bundle (empty-state copy, small animations).

## Recently completed (newest ~10 versions — older detailed entries live in `HEARTH-archive.md`)
- **v323:** **History cleanup — per-item + multi-select delete in the History tab, with sync-proof tombstones; grocery "Clear completed" removed (Cathal request, 11/07/2026; trigger: leftover test items polluting the v306 suggestion strip — his first instinct was to kill history entirely, corrected in design chat: suggestions AND the add-autocomplete read `hist` exclusively, done items are a separate thing that never feeds them).** **(1) History tab UI:** every row gains a ✕ "remove from history" button beside the existing + (32px circle, `.hist-del`, matching the v307 tap-target lesson); a new "☑ Select" toolbar button enters multi-select mode — tap rows to tick (`.hist-check` circles replace the +/✕), then "✕ Delete (N)" / Cancel in the toolbar (`histSelStart`/`histSelCancel`/`histSelDelete`, state in `_histSelectMode`/`_histSelected`, reset on tab entry via `switchTab`). Deliberately **no "clear all history"** anywhere — Cathal wants grocery history kept as a consistent base; deleting is item-by-item cleanup. Deleted entries drop out of the 🔮 suggestions and the add-box autocomplete immediately (both read `hist`). **(2) History tombstones (`fl4_tomb_hist_<lt>`):** reuses the generic v245 tomb helpers under a `hist_<lt>` namespace via new top-level `histTombKey`/`clearHistTomb`/`deleteHistEntries` — keyed by **lowercase item name** (hist entries have no id), 90-day purge via the shared `purgeTombs`, counted by the sync-health panel for free (same `fl4_tomb_` prefix). Pushed on BOTH channels as `histDeleted` alongside the existing `deleted`; both receive paths (`applyPersonal` + household) merge it via `mergeTombs` before the hist merge. `mergeHist` gained a third `histTombs` param and drops any entry whose tombstone is at/after its `lastUsed` (same rule as `mergeListItems`) — so re-adding an item revives it: `addToHist`/`addToHistFor` clear the local tombstone and stamp a fresh `lastUsed` that outlives the partner's tombstone copy. **NOT exported** (matches the fl4_tomb_* export policy), and `importBackupData` now passes local hist tombstones into its `mergeHist` call — **deliberate: restoring a backup does NOT resurrect deleted history entries** (within the 90d window); restored *list items* still resurrect per v288, unchanged. Mixed-version household: a v322 device ignores `histDeleted` and may re-push a deleted entry, but v323 devices keep filtering it via their tombstone — converges per-device, same rollout class as v245. **(3) Grocery "Clear completed" hidden** (one-line guard in `renderList`: `done.length && currentList !== "grocery"`): Cathal wants the grocery done list kept as a consistent record; per-item ✕ delete and "Reopen all" still work there, and todo/travel ("Clear packed")/personal keep their buttons unchanged. **Home-screen live clock + phone/browser back-button navigation (Cathal request, 10/07/2026; design confirmed via 2 AskUserQuestion answers: Ireland time shown only when it differs from local — i.e. auto-appears while travelling — and back button scoped to top-level sections only, not sub-tabs/modals).** **(1) Clock:** new `homeClock`/`homeClockIE` elements under the Home greeting/date; `updateHomeClock()` formats local time via `toLocaleTimeString("en-IE", {hour:"2-digit",minute:"2-digit"})` (matching the existing `en-IE` pattern used elsewhere) and the same call with `timeZone:"Europe/Dublin"` for home time — if the two formatted strings differ, a small "Home (Ireland) HH:MM" line appears, otherwise it's hidden. Driven by its own `_homeClockTimer` (15s interval, started/stopped via `startHomeClock()`/`stopHomeClock()` from within `switchSection`'s home branch/top) — **deliberately not reusing the shared Train-section `ST`/`clearST` timer**, which a Train timer stopping would otherwise silently kill. **(2) Back button:** the app had zero navigation history before this (`switchSection` was a pure in-memory `display:block/none` toggle, no `pushState`/`popstate` anywhere). `switchSection` now pushes `{hearthSection: section}` via `history.pushState` (or `replaceState` for the very first call, so boot doesn't leave an extra empty entry) whenever the section actually changes; a new top-level `popstate` listener reads the popped state and calls `switchSection()` again, guarded by a `_navFromPopstate` flag so the popstate-triggered call doesn't push a further entry (which would break forward/back). Scope is **top-level sections only** (Home/Lists/Train/Recipes/Baby/Trips/Sports) — Lists' History/Notes sub-tabs and modals like the Trip editor are unaffected and still use their own close controls. Works via Android's hardware back button in a PWA; iOS has no hardware back button, so there this only affects the in-browser back gesture. No data-model, export, or sync changes.
- **v321:** **Fix v320 trip deep-link — string/number id mismatch silently fell back to the trips list (Cathal-reported: "still not linking to the specific trip").** Root cause: `row.dataset.id` (the Today-card trip row's DOM `data-id` attribute) is always a string, but trip ids are created as numbers (`Date.now() + Math.floor(Math.random()*100000)`, the `tpSave` handler in `openTripEditor`); `renderTripDetail()` does a strict `x.id === _tripOpenId` lookup, so a string `_tripOpenId` never matched any trip and the function's own `if (!trip)` guard silently redirected back to `renderTrips()` — same end state as the pre-v320 bug, just via a different path, so it looked like the v320 fix hadn't landed at all. One-token fix: `_tripOpenId = Number(row.dataset.id)`. No data-model, export, or sync changes.
- **v320:** **Today-card trip row now deep-links to that trip, not the trips list (Cathal follow-up, 10/07/2026, small well-defined change).** The `renderTodayCard()` trip row names a specific upcoming trip with a countdown (e.g. "Paris in 3 days") but tapping it previously always reset `_tripView` to `"list"` and called `renderTrips()` — landing on the trips home page instead of the named trip. The row's `id: t0.id` is now carried through as a `data-id` attribute (escaped via `esc()`); the `.today-row` click handler's `act==="trips"` branch checks it and, when present, sets `_tripOpenId`/`_tripView="detail"` and calls `renderTripDetail()` directly, falling back to the old list behaviour only if no id is present. Section-switcher/sidebar navigation to Trips is untouched and still opens the list, which is the correct entry point there. No data-model, export, or sync changes.
- **v319:** **Sports homepage entry point (Cathal request, 10/07/2026; design confirmed via 1 AskUserQuestion answer: Today-card row, not a permanent tile).** Cathal had hidden Trips from the bottom nav via the Settings toggle and was relying on the existing "Today" summary card row (v302, `homeTodayCard`/`renderTodayCard`) — which shows an upcoming-trip line only when one exists — to open Trips instead. Sports had no equivalent, so hiding its (already-opt-in, default-off since v308) bar icon left no way back in short of Settings. Added a matching self-suppressing row: iterates `getSportsTeams()`, reads each team's cached next fixture (`sportsCache()[id].next[0]`, populated only once the Sports tab has been opened — same "device-local, pull-on-open" limitation as the rest of Sports), picks the soonest kickoff across teams, and shows "⚽ *Team* vs *Opponent* (H/A) · *when*" via the existing `sportsSummarise`; tapping the row calls `switchSection("sports")` (no extra render call needed — `switchSection` already re-renders Sports, unlike Trips which needs its list-view state reset). Suppressed entirely when no team has a cached fixture. No data-model, export, or sync changes — reuses `getSportsTeams`/`sportsCache`/`sportsSummarise` as-is.
- **v318:** **Nav consolidation — Option A (backlogged 01/07/2026; design confirmed 09/07/2026 via 2 AskUserQuestion answers: Option A over Option B now, and all three agreed stages shipped in one version rather than three).** Bottom bar 7 → 5: `Home · Lists · Recipes · Baby · Trips`. **(1) Notes is now the 5th sub-tab inside Lists** — new `data-lt="notes"` button on the typeBar shows the existing 4-tab notes section (Personal/Work/Log/Food, `renderNotesSection`) in place of the list UI; the Notes bar button, its `HIDEABLE_SECTIONS` entry and its Settings toggle are gone, and `switchSection("notes")` remains as a legacy deep-link shim (maps to Lists + notes sub-tab). Notes has no `LIST_CONFIG` entry, so `renderList()` gained an early-return guard — sync callbacks call it regardless of the active tab and it would otherwise crash on `LIST_CONFIG[currentList].key` — and `applyListTheme` hides the header item-count badge on the notes tab (same leak class as v314). The swipe-between-lists gesture picks Notes up automatically (it walks the visible typeBar buttons). Removed as dead code: the legacy `notesFullPanel` "global notes" view inside Lists + `renderNotesFull` (unreachable — no typeBar button ever selected it — and already broken: its + button had no listener; it showed the same `fl4_notes_global` store the Personal tab shows, so nothing is lost). **(2) Train is off the bar by default** (Sports precedent): default flipped in the `sectionVisible` init PLUS a one-off boot migration (`fl4_trainNavMigrated` flag) so devices that had stored `train:true` pre-v318 also start bare; the Home Training card stays the entry point (`switchSection("train")` works regardless of bar visibility) and the Settings toggle now means "pin Train back on the bar" (settings copy updated to say so). **(3) Trips header gained a "✈️ Packing" button** (the deferred v275 item) jumping to Lists → Travel by programmatically clicking the travel type-btn, reusing the full typeBar switch handler (search/filter resets, unseen clearing). No data-model, export, or sync changes anywhere in this version. Option B (thematic regroup) stays a future exploration — revisit only after Option A beds in. **Tooling in the same PR (Cathal asked about model economy):** new `.claude/agents/hearth-scout.md` — a **Sonnet, read-only code-location subagent** for "where is X / does X exist / who calls Y" sweeps, so expensive main-session models stop burning context on search output (preflight/verifier were already Haiku); CLAUDE.md workflow step 2 now points at it, with the explicit caveat that the main session must still read the exact lines it edits (the scout finds code, it doesn't vouch for understanding it — this session's `LIST_CONFIG` crash-guard catch came from direct reading). CLAUDE.md's model-check rule also gained the inverse direction: a Fable/Opus session that turns out to be doing a small well-defined change should say "this would have been fine on Sonnet" in the wrap-up so Cathal can calibrate session choices — never stopping work over it.
- **v317:** **Week plan — trimmed-down meal planner (from an external review's full-meal-planner suggestion, stripped per triage; design confirmed 09/07/2026 via 4 AskUserQuestion answers: household-shared · dinner-only, rolling next-7-days · lives inside Recipes · per-recipe Add-to-grocery kept unchanged).** "📅 Week plan" button at the top of the Recipes list opens a third recipe view: seven tappable day cards (Today / Tomorrow / "Thu 11"). Tap an empty day → bottom-sheet recipe picker (favourites first, fuzzy search); tap an assigned day → sheet with servings stepper / open recipe / swap / remove. **"🛒 Add week to groceries"** groups the week's assignments by recipe, SUMS servings across days (same recipe Mon+Thu at 4 each → one call at 8), and batch-calls the existing `addRecipeToGroceries` — zero new grocery/merge logic (new optional `quiet` third param suppresses its per-recipe toast; behaviour unchanged for all existing callers), and week-added items feed the v303/v306 replenishment history for free. Deliberately excluded, matching prior rejections: drag-and-drop (fiddly on touch), nutrition APIs / recipe photos (third-party dependency for a nice-to-have), lunch/breakfast slots (v1 is dinner-only). **New shared store `fl4_mealplan`** — one entry per `"YYYY-MM-DD"` day (`recipeId`, `servings`, `updated`, `addedBy`); the day string itself is the merge key: union of days + newest-wins by `updated` per day on BOTH channels with convergence re-push, **no tombstones** (clearing a day writes `recipeId:null` with a fresh stamp), entries >14 days old pruned on save/merge via a deterministic date cutoff so devices can't re-push-flap. Exported + daily-cloud-backed-up + import-merged (import reuses the same newest-wins merge, so restoring an old backup can't clobber a newer plan). Known deliberate limit: two *different* recipes sharing an ingredient already on the list re-tag it without summing amounts — that's Add-to-grocery Phase 3 (pack quantities), still on hold. New top-level: `getMealPlan`, `saveMealPlan`, `pruneMealPlan`, `mealPlanCutoffDay`, `setMealPlanDay`, `mergeMealPlanData`, `addWeekToGroceries`, `renderMealPlanner`, `mealPlanDayLabel`, `openMealPlanPicker`, `openMealPlanDaySheet`. (Naming note: distinct from `fl4_saved_meals`, the food-log's saved meals.)
- **v316:** **Weather card shows the actual nearby town instead of "Current location" (backlog item, captured 03/07/2026; design confirmed 09/07/2026 via 2 AskUserQuestion calls: pick this item over the other ready candidates, then the offline-table approach over a manual Settings field).** New `HW_TOWNS` table (~37 Irish towns/cities + approximate lat/lon) and `hwNearestTown(lat, lon)` — a plain Haversine nearest-match, no network call, no third-party geocoder. In `fetchWeather`'s success handler, when the device's own GPS coords were used (`isDevice` true), `rec.loc` is now `hwNearestTown(lat, lon) || "Current location"` — a real town name when within 20km of one of the table's entries, otherwise the previous generic fallback (e.g. abroad, or a rural spot away from any listed town). The non-device fallback path (`"Carrigaline"`, used when geolocation is denied/unsupported) is unchanged. Device-local only — weather is never synced, so no data-model/export/sync change.
- **v315:** **Recipes list opens on ★ Favourites by default (backlog item, captured 09/07/2026; design confirmed same day via 2 AskUserQuestion calls: fall back to All when no favourites exist, and only change the initial default — not a reset on every tab visit).** `_recipeFilter` (the module-level global driving the list's filter chip bar) now initializes to `"fav"` instead of `"all"`. A new guard at the top of `renderRecipes()`, right after `hasFav` is computed, resets it back to `"all"` for the session if no recipe is currently favourited — so the list never opens on an empty "Nothing here yet" screen with no Favourites chip visible to explain why (the Fav chip itself is only rendered in the bar when `hasFav` is true, same as before). Chip persistence for the rest of the session is unchanged: once you manually tap a different chip, it still sticks until reload, same as pre-v315 behaviour — this only changes the starting point. No data-model, export, or sync change.
- **v314:** **Header item-count badge no longer leaks onto other sections (backlog item, captured 09/07/2026).** `switchSection` already hid `#hdrBadge` on every non-Lists section, but `renderList()` unconditionally re-showed it whenever it ran — so a background sync event (e.g. `fetchHousehold`/`fetchPersonal` callbacks, which call `renderList()` regardless of which section is on screen) could bring the badge back while Home/Baby/etc. was open. One-line guard in `renderList()`: the badge only shows when `currentSection === "lists"` (same `currentSection === "X"` idiom already used throughout the file, e.g. the list-swipe handler's `currentSection !== "lists"` guard). Notes currently lives as its own top-level section (`switchSection`'s `"notes"` branch, separate from `"lists"`) rather than as a Lists sub-tab, so no special-casing was needed there. No data-model or sync change.
- **v313:** **Weather card: High/Low and Rain% positions swapped (Cathal request, 09/07/2026).** The Home weather card's meta block now reads Feels → H:/L: → ☔ Rain % → Humidity → "as of" timestamp (was Feels → Rain → H/L since v305). One concatenation-order change in `hwPaint` — `popStr` moved after the H/L segment; no data, fetch, or cache changes. **Tooling in the same PR:** tightened `.claude/agents/hearth-verifier.md` check 3 after the v312 CI incident — the exactly-one-version-string check is now explicitly zero-judgement (a second `vNNN · DD/MM/YYYY` match FAILs even when it's "only" a code comment; in v312 the verifier waved exactly that through and the GitHub Action then red-✗'d the PR). Code comments must use the placeholder form `vNNN · DD/MM/YYYY`. Also noted for the record: v312's post-merge GitHub Pages deployment failed transiently (known GitHub flake) and succeeded on the next push to main — no action ever needed beyond re-triggering/waiting.
- **v312:** **Cold-start opening-flash fix — boot splash + version gate (Cathal backlog item 03/07/2026; design confirmed 06/07/2026: "smart" 4s→8s wait budget + dark splash matching the Android launch screen).** A full-screen `#bootSplash` overlay (inline-styled, first element in `<body>`: `#1C0A04` background + app icon + pulsing dots) covers the app from first paint; a new boot gate in the SW section of the script decides when it's safe to reveal. **Zero sw.js logic changes** (VERSION bump only) — the gate reuses the existing `GET_VERSION`/`SW_VERSION`/`SW_UPDATED` postMessage plumbing. Hide-immediately paths: `_isPreview`, no SW support, no `navigator.serviceWorker.controller` (first visit / hard refresh — the page came straight from the network), SW registration/update failure, offline. Otherwise a 4000ms deadline runs, extended once to 8000ms total (splash shows "Updating…") when a new SW is actively installing (`updatefound`/`reg.installing`). `PAGE_VERSION` is captured from the static `#appVersionLabel` text at gate setup, before any SW message overwrites the label — so **no new version-bump site**. Versions compare **numerically** via `_verNum` ("v312 · date" → 312): SW newer than page → the fresh shell is already in the SW cache → one **silent reload behind the splash** (`bootStaleReload`, sessionStorage `hearthBootReloaded` loop guard; a second attempt in the same session falls back to hiding the splash + the banner). Page newer than SW (fast network served fresh HTML while the old SW was still active) → keep waiting for `SW_UPDATED` to confirm, NO reload — **a plain `!==` comparison here would have wasted a reload on every good-network update open; caught during build.** Versions equal + `reg.update()` settled + nothing installing → confirmed current, splash fades (~180ms) and the loop-guard flag clears. Mid-session updates after the splash hides keep the existing tap-to-refresh banner, now improved: `SW_UPDATED` only shows the banner when the activated version actually differs from the page (previously a spurious "Update available" could appear when the page already WAS the new build). Worst case (update can't finish inside 8s on a bad connection) is unchanged from today — old version + banner — just behind a longer splash. The v295 lie-fi 3.5s shell race and all offline behaviour are untouched.
- **v311:** **Fix Sports section "Maximum call stack size exceeded" crash (Cathal-reported bug).** Root cause: `fetchSportsForTeam` synchronously invoked its callback (`renderSports`) whenever a fetch for a team was already in flight; `renderSports` unconditionally calls `refreshSports(false)` at its end, which — since the team's cache still wasn't populated (the original fetch hadn't resolved) — called `fetchSportsForTeam` again, hitting the same synchronous-callback branch again, and so on, an unbounded synchronous recursion with no async boundary to break it. Triggered by re-rendering/reopening the Sports tab while a team's fetch was still pending (e.g. slower network, quick tab switch away and back). Fix: the already-fetching branch now just returns without calling back — the in-flight fetch's own `step()` already calls `cb()` once when it resolves, so a concurrent caller doesn't need (and must not trigger) another render. One-line change in `fetchSportsForTeam`, no data-model or UI change.
- **Tooling, no version bump (05/07/2026):** **Workflow automation.** Added `.claude/agents/hearth-preflight.md` (Haiku subagent, run BEFORE editing: confirms base = latest origin/main, pushes `backup-vNNN`, reports version state, greps for already-built features, lists backup branches outside the keep-10 window) and `.claude/agents/hearth-verifier.md` (Haiku subagent, run AFTER every edit: `node --check` on the extracted main script block, file-integrity checks, version-bump consistency; verdict SAFE TO COMMIT / DO NOT COMMIT). Added `.claude/commands/release.md` (`/release`: bump both files, update notes, verify, commit, push, open the PR), `.claude/skills/hearth-council/SKILL.md` (full 7-expert council spec, loaded only on "use my council"), and `.github/workflows/hearth-pr-checks.yml` (zero-token CI backstop running the verifier's mechanical checks on every PR; to make it merge-BLOCKING Cathal must add a branch-protection rule on `main` requiring the "Hearth checks" status — until then a failure just shows a red ✗ on the PR). **Split this file:** detailed changelog v230–v298 moved to `HEARTH-archive.md` (~101k → ~52k chars read per session; read the archive only when an old version is referenced). CLAUDE.md workflow slimmed to delegate steps to the two subagents. Also added the **model-check rule** to CLAUDE.md (Cathal's request): sessions must state their model and prompt him to restart on Fable/Opus before building anything risky (sync, dosing, trip import/export, service worker) or design-fuzzy; plain Sonnet stays the default for small well-defined changes. App files untouched — the app stays v308.
- **v310:** **Growth chart redesigned to one weight/height-vs-WHO-median ratio chart (Cathal follow-up on v309, same day).** Replaced the two separate Weight/Height mini-charts with a single chart: the ratio of logged weight to the WHO weight-for-length median for that reading (the same ratio `growthBand()` already uses for the per-entry Below/Normal/Above label), plotted as a **percentage** over time, with a **shaded "Normal" band (90–110%)** and a dotted 100% line marking the WHO median itself. Design confirmed first (2 AskUserQuestion calls): percentage y-axis (not raw decimal ratio) and reuse the existing 90/110 thresholds rather than picking new ones. Refactored `growthBand()`'s internal ratio calc to percentage and pulled the thresholds out into shared top-level constants `GROWTH_BAND_LO`/`GROWTH_BAND_HI` (90/110) so the list labels and the chart band can never drift apart. Replaced `growthChartSvg()` with `growthRatioChartSvg(points, color)` (draws the band rect + median dotted line + ratio line/dots) and rewrote `growthChartsHtml(bd)` to build the one chart from entries with both weight+height logged. Needs `babySex` set (the ratio needs a WHO median lookup) — shows an explanatory placeholder if not, or if fewer than 2 qualifying readings exist yet. No data-model, export, or sync changes.
- **v309:** **Visual growth chart (backlog item, picked up 05/07/2026).** The plotted SVG "trend line" Cathal originally pictured, alongside the v301 text band-trend (kept as-is). Two stacked mini line-charts in the Growth view — **Weight** and **Height** — sit right below the band-trend summary and above the measurement list. Design confirmed first (3 AskUserQuestion calls): both metrics charted (not a toggle), a dotted WHO weight-for-length **median overlay** on the weight chart only (computed per-point via the existing `growthMedianForLength`, since that's the only WHO table in the app — no equivalent height-for-age table exists), placed above the list rather than below it. New top-level `growthChartSvg(points, medianPoints, color, unit)` (hand-built inline SVG — no charting library, single-file app) and `growthChartsHtml(bd)` (builds both charts from `bd.growth`, ascending by date; line colour follows `babySex`, girl/boy/neutral). Each metric independently falls back to a "log a couple more readings" placeholder below 2 points (a line needs ≥2 to mean anything); the whole section is skipped when there are no measurements at all (the existing "No measurements yet" empty state still covers that). Chart section only appears in the Growth view — no data-model, export, or sync changes.
- **v308:** **Sports section — hideable pull-on-open scoreboard (Cathal request; design + API verified first).** New 8th nav section 🏅/⚽ **Sports**, **default HIDDEN** (opt-in via Settings section toggle, so it doesn't add an 8th icon for anyone who doesn't want it — Cathal enables it). Per configured team: **next fixture** (vs opponent, H/A, local kickoff time, competition) + **last result** (W/L/D + score). Data from **TheSportsDB free key "3"** — Cathal verified in his browser that `eventsnext.php` returns fixtures on the free tier (the patron-tier risk). Endpoints: `eventsnext.php?id=`, `eventslast.php?id=` (returns `results` or `events`), `searchteams.php?t=` (config). **Device-local, NOT synced/exported:** `fl4_sports_teams` (list; seeds with Man Utd 133612 until edited) + `fl4_sports_cache` (per-team next/last + `at`). Refresh on tab open, cache TTL 2h, offline shows cache. **Manage teams** modal: search by name → add, × to remove. **Hard limits (by design, stated in-card "updates when you open this tab · not live"):** no live scores, no alerts (push is ruled out). Coverage reality (told Cathal): soccer/national teams good (Man Utd, Inter Miami, Argentina, Ireland); GAA + golf NOT covered by the free tier and out of scope. New top-level: `getSportsTeams`/`saveSportsTeams`, `sportsCache`/`saveSportsCache`, `sportsWhen`, `sportsSummarise`, `fetchSportsForTeam`, `refreshSports`, `renderSports`, `openSportsManage`. Wiring: nav button `bnSports`, `sportsSection`, `HIDEABLE_SECTIONS`+sports (default false), `applySectionVisibility` navIds, `switchSection`, settings toggle `data-sec="sports"`. Cross-origin API → SW fetch handler ignores it (same-origin only); no SW/caching change. **First non-utility (leisure) feature + first standing third-party data dependency beyond weather — accepted; free-API longevity is a known risk.**
- **v307:** **Suggestion chip tap-target fix + empty-list bug (Cathal: v306 add/dismiss too small & close).** (1) Rebuilt each grocery-suggestion chip as **two large, divider-split targets**: the whole left block (+ · name · reason, ~40px, `padding:9px 13px`) adds; the right × (own ~40px block, `border-left` divider) dismisses. Removed the cramped tiny-+-beside-tiny-× so they can't be mis-tapped. (2) **Bug found while testing:** the v306 strip was injected only in the populated-list path, so it never showed on an **empty** grocery list — exactly when suggestions are most useful. `suggHtml` now computed before the empty-list early-return and prepended there too; chip wiring extracted to shared `wireSuggestionChips()` called from both paths.
- **v306:** **Predictive replenishment suggestions UI — step 2 of 2 (visible half of the v303 foundation).** A "🔮 Suggested" chip strip at the **top of the grocery list only** (suppressed while searching). Two tiers from the add-date history: **interval-based** when an item has ≥3 recorded `dates` (median gap → suggest when `daysSince >= gap*0.9`, reason "usually ~Nd"); **frequency fallback** (`count>=3` and not added in 10+ days, reason "often bought") so it's useful immediately before the v303 date series fills in. Excludes items already on the list; max 4, ranked by score (interval matches outrank frequency). **Never auto-adds** — tap a chip (or its +) to add via the shared `addItemToCurrent`, tap × to **snooze that item 14 days** (`fl4_sugg_snooze`, device-local + transient, NOT exported/synced). New top-level: `buildGrocerySuggestions`, `buildSuggestionsStrip`, `medianGapDays`, `suggSnoozeMap`/`suggSnooze`; injected in `renderList` as `suggHtml + html` with chip wiring alongside the existing handlers. Grocery-only by design (replenishment cadence is a consumables concept; todo/travel don't fit). **This completes unrequested feature #1** (foundation v303 + UI v306); both unrequested features now shipped (voice = v304).

## Recipes section — current shape
- Store: `fl4_recipebook` — **shared household-wide** (newest-wins-by-`updated` merge via `/shared`, plus same merge on the personal channel for same-account devices); separate from grocery-import `fl4_recipes`.
- Section headers: `<b>…</b>` / "Label:" lines render as sub-headings in the detail view and are excluded from the grocery list (`isHeaderIng`).
- List view: filter bar (All / ★ Favourites / per-category chips), grouped by category in All view.
- Detail view: servings stepper, ingredients (scaled per unit rules), method, notes, fav star, **🛒 Add to grocery list**, Edit/Delete.
- Editor: name, serves, category (datalist-suggested), ingredients (one per line "amount unit name"), method, notes.
- Import: ⬇ Import button → paste CSV → live "N recipes found" preview → Import.
- Unit rules: tsp/tbsp fixed; cups→g (dry) or ml (liquid); counts pluralise.
- Add-to-grocery: tag = recipe name; **amounts** carried as a separate `item.amount` field on grocery items, scaled to current servings (names-only fallback when no clean amount); saves a reusable chip at base servings.
- Week plan (v317): third recipe view ("📅 Week plan" button on the list) — 7 day cards (today..+6, dinner only), tap-to-assign from a favourites-first picker, per-day servings, "Add week to groceries" batch-calls the per-recipe flow with summed servings. Store `fl4_mealplan`, household-shared (newest-wins per day, no tombstones).

## Pending / next steps (not yet built)
- **Family Activity Log (backlogged 06/07/2026 from Cathal-supplied feature proposals; needs its own design chat before build).** A read-only, chronologically day-grouped "family story" view aggregating significant events from data the app already timestamps: Baby growth/medicine/milestones/teeth, completed Train sessions, trip starts/ends, created notes (`createdAt` exists since the notes rework). Zero new logging, zero new data model, no sync changes. Agreed guardrails from the triage: **hideable section, default hidden** (v308 Sports precedent — no 8th permanent nav icon); significant events only (grocery adds would spam it); ~90-day default window + "load earlier"; medicine rows factual-only ("Calpol 5 ml · 18:40 · check the leaflet") deep-linking into Baby — **all dosing/interval logic stays exclusively in the Baby flow, never duplicated here**; rows deep-link to their source section via existing `switchSection`/modal patterns. v1 scope call from the triage: plain feed + filter chips, **no search** (leaner first cut than the proposal's wireframe). Design chat must settle: exact event inclusion list, entry point (hideable section vs Home teaser line), filters, mobile layout. Reflective nice-to-have, not daily utility — does NOT jump the queue over Cathal-requested items (per-person packing checks, nav consolidation; cold-start flash shipped as v312). Baby-related wording goes through council review per the safety rules.
  - **Triage of the other two proposals in the same document (06/07/2026 — do NOT re-chase):** "Hearth Intelligence" daily briefing → **rejected as a feature**: it's the v302 Today card + v306 suggestions + v291 interval advisory re-described; the genuinely new lines are either nags (v302 deliberately chose positive-only), need a nonexistent passport data model, or are surveillance-flavoured pattern-mining ("wife adds yogurt on Fridays"). If a single line ever proves wanted (e.g. "workout due, last done Nd ago") it's a small Today-card addition, not a system. "Hearth Lens" on-device AI vision/OCR → the document itself rejects it and the reasoning is sound (breaks single-file/no-dependency/mid-range-phone constraints); its suggested alternatives are already shipped or backlogged. No action on either.
- **Backlog captured 09/07/2026 (Cathal, awaiting build):**
  - ~~Header item-count badge shows on irrelevant pages.~~ **Done in v314.**
  - ~~Recipes: open on ★ Favourites by default.~~ **Done in v315.**
- **Backlog captured 03/07/2026 (reviewed with Cathal, awaiting build):**
  - ~~Current-city on weather card.~~ **Done in v316** — offline ~37-town Irish table + local nearest-match (option a).
  - **Sports fixtures/results card:** feasible ONLY as **pull-on-open** (like weather) — NOT live alerts, because push notifications are ruled out; hard ceiling, set expectations. Needs a sports API (third party, but only *requests public data* — softer than location; weather set the third-party precedent). TheSportsDB free no-signup tier = best keyless fit; football-data.org richer but needs a key + tighter limits. Scope to a few favourite teams configured in Settings; own hideable section or Home card. Caveats: first *leisure* (non-utility) feature; free-API uptime/longevity dependency risk; no goal alerts. Lower priority than family-utility items; wants a design chat (API choice, team-config UX, "no push" expectation).
  - **Per-person travel packing checks** (Cathal's idea) — three checks (Cathal/Petra/daughter) per travel item; opt-in per item; needs design chat (display at 480px, done-semantics, sync).
- **4th external review (received 03/07/2026, reviewed v296 — logged for reference; Cathal flagged it may be stale/lacking full context, and it is).** Its headline recommendation — "make v297 the Train dedupe" — was **already shipped as v297** before this review arrived; likewise it re-flags the field-wise-merge nested-collection limitation that v296 already documents as a known accepted gap. **Nothing new actionable except one item worth noting:** it spotted a **duplicated `setSheet`/`closeSetSheet` + `DOMContentLoaded` + `adj` wiring block** (~line 7592 and ~9545 regions) beyond the 7 functions v297 removed — harmless today (guarded DOMContentLoaded listeners) but the same copy-paste class; **candidate for a small future dedupe pass IF that area is touched.** It also (incorrectly, for a stale copy) claimed the `trainTodayStr` bodies had diverged with extra `getWD`/`saveWD`/`getWorkoutForDate` helpers — v297 verified the 7 pairs were byte-identical before deleting, so treat that as review-of-old-code noise. Everything else in it is already-done, already-documented, or the same correctly-rejected modularisation/IndexedDB suggestions. Its genuinely useful forward ideas (per-entry `updated`+union merge for growth/medicine/bookings arrays; sync-health panel — already Tier 2) are captured below / in the Tier 2 queue.
- **Deferred sync hardening (from reviews 3 & 4, needs design confirm):** extend the v245 per-item `updated`+union+tombstone merge down into the nested Baby collections (growth/medicine/milestones) and trip bookings, so concurrent same-collection edits across the household channel stop resolving whole-collection remote-wins. Bigger change — touches every mutation site (must stamp `updated`) plus export/import/merge. Only worth it if a real dropped-dose/dropped-booking incident occurs; the personal channel's union usually self-heals it today.
- **Travel list: per-person packing checks (Cathal's idea, 03/07/2026 — backlogged, needs its own design chat).** Three separate checks per travel item — Cathal / Petra / daughter — so "passports ×3" can be tracked per person. Cathal's own concern: phone display space. Initial feasibility take: three small initial-circles CAN replace the single checkbox on the travel list (~3×24px fits at 480px; show "2/3" state), but the design questions are elsewhere: (a) not every item is per-person (sun cream is shared) → likely opt-in per item, not global; (b) for the baby it means "packed for her", which changes wording; (c) data model adds fields to list items (e.g. `checks:{}`) → the v296 field-wise merges make this survivable, but concurrent different-person checks on the same item still resolve whole-item newest-wins — one partner's check could lose in a same-second race (probably acceptable); (d) done-state semantics: item complete when all applicable checks ticked.
- **Council worklist (from the v285 full-app council review — being tackled in order):**
  1. ~~Offline app-shell fix~~ — **done in v286.**
  2. ~~Backup resilience (auto-copy to Firebase `/backups`)~~ — **writer done in v287, restore UI done in v288.**
  3. ~~Medicine dose-interval advisory + weight-date~~ — **done in v291** (design confirmed first; log-time confirm + always-visible summary + 24h max all accepted).
  4. ~~Housekeeping bundle~~ — **done in v289** (DOMPurify vendored, tombstones 30d, pre-v242 recipes stamped, touch targets + dose-note text).
  5. ~~Cathal manual check: Firebase rules~~ — **done 02/07/2026**: rules verified (`/hearth` read+write require `auth != null`, everything else denied by default) **and sign-up disabled** in Firebase Auth (Authentication → Settings → User actions), so no new accounts can ever be created. **The v285 council worklist is now fully complete.**
- ~~Nav consolidation — Option A~~ — **done in v318** (all three stages shipped together in one version; design re-confirmed 09/07/2026). **Option B (thematic regroup: Food = Grocery+Recipes, Tasks = General+Personal+Notes, Travel = Trips+Packing) remains a possible future exploration** — cleanest IA but highest churn (splits list machinery across three sections); explicitly considered and deferred again on 09/07/2026: revisit only after Option A beds in, and only with a full design chat + council first.
- ~~Nav cruft (test list types + dormant Baby list)~~ — **done in v294.**
- **Add-to-grocery Phase 3 — real-world purchase quantities.** Convert cooking amounts to shop quantities, e.g. "1200ml oat milk" → "2× 1L bottles". **Council-reviewed; on hold.** Consensus = build thin + advisory (static in-code staples table, render-time from stored numeric amount, show both cooking + suggested pack, only mass/volume convert) — but a real logged dissent (Devil's Advocate) that the coverage cliff + upkeep may not clear the bar. Three open calls before building: show-both vs replace; table scope (~15–20 Irish staples); whether to heed the dissent.
- ~~**Manual amount editing.**~~ **Done in v272.**
- ~~**Pre-v242 recipes are unstamped**~~ — **done in v289**: one-off boot migration stamps them with a fixed constant.
- **Imported recipes default to "Uncategorised"** — Cathal to categorise over time (the v249 bulk-categorise tool makes this fast: filter to Uncategorised → Select → set).
- ~~**Trips: expand/collapse all booking cards.**~~ **Done in v284.** An "Expand all / Collapse all" toggle (or tap-once-to-expand-all gesture) in the trip detail header. When expanded, all cards show their ref, notes, and Maps link at once — useful for a quick pre-trip scan. Currently each card is tapped individually to expand. Implementation: a toggle that sets all keys in `_tripExpanded` to true/false and re-renders.
- ~~**Trips: scroll restoration on booking open / back.**~~ **Done in v285.**
- ~~**Possible future backup enhancement:** an auto-copy into Firebase `/backups`~~ — **done in v287** (daily, keep-7). Still open if ever wanted: a reminder independent of app open (current nudge is on-open only).
- Known minor parser gaps: compound amounts like "1 ¾ cups + 2 tbs Greek yogurt" keep the whole string as the name (don't scale → land as name-only on grocery); "Garlic - 2 cloves" (name-first) doesn't split the amount cleanly. Rare; acceptable.
- Open question (low priority): whether to sync the travel tag pool / recipe categories across devices (currently localStorage, so per-device; per-item tags DO sync).

**Resolved since last update:**
- ~~Recipe deletions don't propagate~~ → **fixed in v268** (tombstone pattern, mirrors v245 list tombstones).

## Shipped (reverse order, condensed — full detail in the version log above for the newest ~10, in `HEARTH-archive.md` for older)
v323 history per-item + multi-select delete (synced name-keyed tombstones) + grocery Clear-completed removed · v322 home live clock + back-button nav · v321 fix trip deep-link id type · v320 Today-card trip deep-link · v319 Sports Today-card entry point · v318 nav consolidation Option A (bar 7→5: Notes into Lists · Train off-bar by default · Trips packing link) · v317 Week plan dinner planner (7-day tap-to-assign + add-week-to-groceries, household-synced) · v316 weather card nearby-town name (offline Irish town table) · v315 Recipes opens on Favourites · v314 header badge leak fix · v313 weather card H/L–Rain% swap · v312 cold-start boot splash + version gate · v311 Sports recursion crash fix · v310 growth chart redesign (single WHO-ratio chart) · v309 visual growth chart · v308 sports scoreboard section · v307 suggestion-chip tap targets · v306 predictive grocery suggestions UI · v305 rain % on weather card · v304 voice capture (speak-a-list) · v303 replenishment data foundation (invisible) · v302 Today summary card · v301 growth-trend summary · v300 autofill/password-prompt fix · v299 sync-health panel · v298 trips departure badge · v297 train dedupe · v296 field-wise baby/trip merges · v295 SW lie-fi fix + tombstones 90d · v294 cruft removal (baby list + test lists) · v293 flight seats field · v292 trips boarding/gate + one-step connections · v291 dose-interval advisory + weight-date · v290 restore-dialog z-order + boot watchdog · v289 housekeeping (DOMPurify vendored · tombstones 30d · recipe stamps · touch targets) · v288 cloud restore + import gaps (trips/notes/tombstones) · v287 daily cloud backup (keep-7) + nudge 30d · v286 offline app-shell fix (network-first + cache fallback) · v285 trips scroll restore · v284 trip expand/collapse all · v283 trip 24hr times + maps link · v282 trip timeline polish · v281 trip import merge into existing · v280 trip JSON export + import · v279 fix timeline same-day ghost cards · v278 timeline view + date separators · v277 connecting flight legs + transfer time · v276 fix booking editor on sync · v275 Trips section · v274 location-based weather · v273 hourly weather strip · v272 manual grocery amount editing · v271 fix dashboard duplicate add · v270 scroll-to-top everywhere · v269 recipe search + scroll restore · v268 recipe deletion sync · v267 feels-like on weather card · v266 version label date-only · v265 timestamp diagnostic bump · v264 weight-based medicine dosing · v263 baby medicine quick-chips + babySex-save fix · v262 weather 3-day sheet · v261 home layout cleanup · v260 front-page ★ Today · v259 weather timestamp · v258 weather card · v257 tap-zone fix · v256 "Today" picks · v255 partner-recipe toast · v254 noindex · v251–253 backup nudge · v250 method formatting · v249 bulk categorise · v248 recipe-editor-no-close · v247 list convergence re-push · v246 grace window removed · v245 union/newest-wins/tombstones · v243 sync send-wedge fix · v242 newest-wins recipe sync · v241 grace catch-up · v240 recipe sync · v237–239 add-to-grocery (names→amounts→headers) · v236 recipe categories+favourites · v234 CSV import · v232 Recipes section.

## Open decisions on record
- **Week plan (v317):** household-shared chosen over device-local (it's coordination data, like grocery/recipes); dinner-only rolling next-7-days (no week navigation, no lunch/breakfast slots in v1); lives inside Recipes (no new nav icon, deliberate given pending nav consolidation); per-recipe "Add to grocery list" kept unchanged alongside it. Merge keyed on the day string (newest-wins) chosen over ids+tombstones so unassignment needs no deletion machinery. Drag-and-drop, nutrition APIs and recipe photos rejected (same fiddly-touch / third-party-dependency grounds as prior decisions).
- Add-to-grocery: **Option B** chosen (push items to Grocery **and** save a reusable chip in `fl4_recipes`). Saved chip stored at **base servings**. Re-tapping **updates the chip quietly + always re-adds the tagged items**.
- Add-to-grocery amounts (Phase 2): **Path B** chosen — amount is a **separate `item.amount` field**, not baked into the name (keeps name as dedupe key, avoids freeform parsing, and gives Phase 3 something to build on). Scale at **current stepper servings**; **new items get amounts, existing items re-tagged only** (never overwrite a hand-typed quantity). Base-vs-current divergence is deliberate: chip = base, grocery labels = current.
- Play Store + push notifications: rejected as disproportionate; app stays personal/family.
- Direct Claude↔GitHub editing: rejected to preserve the manual review checkpoint.
- Recipe sharing: started as **merge-by-id / incoming-wins** (v240), which clobbered edits when a stale device pushed. Replaced in **v242** with **newest-wins by `updated` timestamp + convergence re-push** on both the household and personal channels. Accepted costs: **deletions don't propagate**, and concurrent edits to the same recipe resolve as newest-edit-wins (older lost). Recipes are shared whenever a household is set (no per-section toggle).
- List sync model (v245): **union-by-id + per-item `updated` newest-wins + deletion tombstones**, applied identically on the household and personal channels (replaced the old household wholesale-replace, which lost concurrent adds). Tombstones (`fl4_tomb_<lt>`) are transient sync metadata, 90-day purge (7d→30d v289, →90d v295), deliberately excluded from backup/export.
- Recipe stores kept separate: `fl4_recipebook` (Recipes section) vs `fl4_recipes` (grocery quick-import).
- **"Today" view (v256): Option B** chosen — a manual **pick-for-today focus view**, not a recurring/daily-reset flag. **Persistent (no auto-reset)** so it adds zero new sync machinery (rides the per-item `updated` merge); surfaced as a **filter chip on General**, not a separate tab; **completing an item clears its `today` flag**; scoped to General only. The recurring/auto-clear alternative was rejected to avoid day-rollover + double-reset sync hazards.
- **Accidental-tap fix (v257): neutral-buffer approach** chosen — notes open only on the item body text, with a dead-zone gap beside the checkbox. **Long-press-to-open-notes was rejected** as too fiddly on touch (scroll-cancel, click-suppression, native context-menu collision, weak discoverability). Applies to all detail lists, not just General.
- **Weather location (v274): auto-detect with fallback** chosen — `navigator.geolocation` on every weather fetch, silently falling back to the hardcoded Carrigaline coords (v258) on denial/timeout/no support. **No manual Settings toggle** (rejected as one more thing to remember before/after travel). **Generic "Current location" label** chosen over an extra reverse-geocoding call to show the actual city name (rejected as an unnecessary third-party dependency for a "nice to have").
- **Trips section (v275):** named **"Trips"**, not "Travel" — the existing Lists→Travel packing list already owns that name. **Booking-level sync granularity** chosen over whole-trip newest-wins (a trip is a *container* of independently-editable bookings, so whole-trip replace risked clobbering a partner's edit to a different booking in the same trip — unlike a flat recipe). **Hideable nav entry** (added to `HIDEABLE_SECTIONS`, like Train/Notes/Home) rather than permanently pinned. Nav position: **end of the bottom bar** (confirmed via direct question, options were "right after Recipes" vs "at the very end"). Each leg of a multi-leg journey is its **own separate booking card** (no combined round-trip object). Deferred to a later version: countdown to departure, link to the Travel packing list, per-traveller passport info, budget.


---
---

# Hearth — Architecture & Working Notes

A reference for any new chat working on the Hearth PWA. Read this first.

## What Hearth is
- A family-hub Progressive Web App for two people (Cathal + partner Petra).
- **One single-file vanilla HTML/JS app**: `index.html` (~11,200 lines, ~665k at v308) plus `sw.js` (service worker).
- No build step, no framework. Plain HTML/CSS/JS.
- **Hosting:** GitHub Pages at `coshea321.github.io/miniature-journey`
- **Storage:** localStorage (primary) + Firebase Realtime Database (household sync between the two users)
- **Users:** `cathal1` (Cathal) and `petra`. Items can carry `addedBy` and sync between devices.

## Who maintains it & how
- Cathal is a **non-coder**. He reviews and merges PRs; Claude Code does all the coding.
- Claude Code (CLI / web) works on the file directly, commits and pushes to a feature branch, and **opens a PR immediately** — Cathal reviews the diff on GitHub and merges to `main`. The human-review checkpoint is deliberate; Claude Code never pushes directly to `main`.
- Before each change, Claude Code creates a `backup-vNNN` branch (frozen snapshot of the prior version) for rollback — the `hearth-preflight` subagent does this. Retention: keep the newest 10; older ones are named for Cathal to delete on GitHub (branch deletion is blocked from remote sessions).

## Sections (bottom nav — 5 icons by default since v318)
Home · Lists · Recipes · Baby · Trips (+ Train and Sports as opt-in bar icons via Settings; Notes is a sub-tab inside Lists; Train is always reachable from the Home Training card)

- **Home:** order is greeting → weather → Training strip → Baby strip → quick-add → ★ Today → Grocery → week strip (v261 layout). **Weather card** under the greeting (v258): current temp + condition + humidity + high/low for Carrigaline, via Open-Meteo (no key); device-local, cached in `hearthWeather`, refetched only if >30 min stale; hidden if no data and offline; "as of HH:MM" timestamp always shown (v259); **tap opens a 3-day detail sheet** (v262: feels-like, humidity, per-day high/low + 💧 rain chance + 💨 wind), **plus an hourly scroll strip for the next 24h** (v273: icon/temp/rain% per hour); **feels-like shown on card** (v267); **follows device GPS with Carrigaline fallback** (v274 — label reads "Current location" or "Carrigaline" accordingly). **Training strip** (v261): compact full-width horizontal card (sessions today + kcal), taps through to Train. **Baby strip:** last medicine + latest weight. **"★ Today" to-do spotlight** (v260, moved above Grocery in v261): only tasks starred for today (v256 `today` flag), empty state "🎉 No tasks for today". The old "active items" tile was removed in v261. Grocery spotlight unchanged.
- **Lists:** Grocery, General (todo), Personal, Travel. Collapsible categories, per-item notes/links/due dates, search (fuzzy, toggleable), print, import, duplicate highlighting, partner-added "NEW" indicators, expand/collapse all. Travel has multi-tag items + a tag filter bar (incl. "Untagged"). Grocery has recipe-tag import (tag a batch, clear/revert as a group) with reusable saved recipes. **General has a "Today" pick** (v256): a ☆/★ star per item sets a manual `today` boolean, with a **★ Today (N)** filter chip at the top; ticking an item done clears its today flag; it syncs via the normal per-item `updated` merge (no separate machinery). **Tap zones on item cards** (v257): the circle checks off; the **item body text (`.item-body`) opens the notes sheet**; the gap between them is a deliberate neutral buffer (does nothing) so a near-miss on the checkbox doesn't open notes. Category pill, ★ star, and ✕ delete are their own zones.
- **Train:** yoga flows + physio programmes. Every exercise timed/auto-advancing; one-sided exercises are explicit (Right)/(Left) entries each with their own chime. tsp/tbsp-style timing rules don't apply here.
- **Recipes:** recipe book with ingredients/method/servings/notes, servings scaler, categories + favourites, CSV batch import.
- **Baby:** growth (WHO weight-for-length), medicine, milestones, teeth, packing bags, sex selector.

## CRITICAL technical quirks (these cause bugs if ignored)

1. **38 interleaved duplicate Train functions.** Many Train/body/history functions exist as TWO copies. Edits targeting them must hit BOTH copies — a grep count of 2 is correct and expected. (Baby render functions are single copies.)

2. **Two real `<script>` blocks — syntax-check the RIGHT boundary.** The file has one external script (DOMPurify, self-closed) then the **main inline app block**; a third `<script>` appears only *inside a JS string* (the print-doc) and is not a real block. To check the main app code, extract from the `// PREVIEW MODE` comment to the **FIRST** `</script>` after it, then `node --check`. **Using the LAST `</script>` produces a convincing FALSE error** — it sweeps up the print-doc HTML string and other script blocks, and `node --check` reports a syntax error (often "Unexpected token '<'") that is NOT real. If a syntax error appears, first confirm you used the first-`</script>` boundary before chasing it. **Also: never hard-code the boundary line number from a previous version — every edit shifts line numbers, so a stale `sed -n 'A,Bp'` range will truncate the script mid-code and throw a false "Unexpected end of input". Re-derive the first-`</script>` line dynamically each time (e.g. `awk 'NR>=START && /<\/script>/{print NR; exit}'`) before extracting.**

3. **Python `str_replace` fails silently.** Always grep-verify a replacement actually landed (count occurrences before/after).

4. **Never write at a computed index without checking `find()` succeeded.** The v221 corruption came from a Python edit where `find()` returned −1 and the code then wrote at `idx + len(...)`, which appended a FULL DUPLICATE of the document after `</html>` — doubling the file to ~923k chars (two "Hearth" headers, raw `no-cache` meta visible as text, broken nav). If a find/anchor might miss, assert it found something before slicing/inserting.

5. **End-of-file edits are dangerous — verify structure after.** After ANY edit near the end of the file, verify: total length is sane (**~557k at v267, ~665k at v308**, growing slowly each version — a sudden jump to ~900k+ means the v221-style doubling), exactly ONE real `<html lang>`, ONE version string, DOCTYPE count of 2 (one real + one inside the print-doc JS string). The fix for the v221 doubling was truncating at the first `</body>\n</html>`.

6. **Scope: helpers called from top-level renderers must be top-level.** `renderList`, `openItemSheet`, `switchSection` etc. are top-level functions. A helper they call (e.g. `getTravelTags`, `renderItemSheetTags`) that is accidentally defined INSIDE a setup/IIFE block throws "X is not defined" the moment the renderer runs (this was the v226 bug). Define such helpers at top level.

7. **Bottom-of-page sheet wiring must run after DOM-ready or be null-guarded.** Several sheets (import, recipe import, item sheet) have their HTML near `</body>`, AFTER the main script. Code that calls `getElementById(...).addEventListener` on those elements at top-level script time hits `null` (the v235 crash). Either defer wiring with `DOMContentLoaded` (with an `else wire()` fallback if already loaded) or null-check every element before wiring.

8. **`confirmDialog` is OK/Cancel only.** It does NOT take custom button labels or a third option. When a 3-way choice is needed (e.g. recipe clear: delete added / keep added / cancel), hand-build a small overlay dialog — don't assume confirmDialog can do it.

9. **Check whether a feature already exists before building it.** This codebase has had features added across many sessions; some are present but not in any summary. Twice, work was started on something already built (list search; a collapse-arrow CSS rule). grep for the feature's likely identifiers first.

10. **`var history` conflicts with `window.history`.** Declaring `var history = []` (or any value) in browser global scope silently fails in some Android Chrome versions — `window.history` is a protected browser object and the assignment is ignored, so `history.sort()` later operates on the real `History` API and blows up. **Never use these names as global variables:** `history`, `location`, `name`, `status`, `frames`, `top`, `parent`, `self`. Use `hist`, `loc`, `roomName`, etc. instead. (Discovered building FreshList v3-v4; caused items to disappear after first add.)

11. **Firebase SSE self-echo race condition.** Every local push to Firebase triggers an SSE event back to the same device carrying the pre-push state. If that event is applied naively it overwrites locally-added items — the "items disappear after 8 additions" bug. The v245/v246 Hearth solution (union-merge + newest-wins + tombstones, apply immediately) already handles this correctly. If ever revisiting sync: **do not re-introduce a blanket grace-window that drops all SSE events** — it silently kills incoming changes from other devices. The right model is merge-then-re-push-if-fuller, not drop.

12. **Format toolbar on mobile goes at the bottom.** When building any rich-text editor with a toolbar, put the toolbar at the **bottom** of the screen. Android's native text-selection popup (Copy/Cut/Paste) appears near the selected text, which is usually in the upper part of the editor — directly over a top toolbar. Bottom toolbar avoids the collision entirely. Use `padding-bottom: max(10px, env(safe-area-inset-bottom))` to respect the home indicator on notch devices.

13. **PWA manifest icons must be real PNG files with separate `any` and `maskable` entries.** Using SVG data-URIs in manifest `icons` or combining `"purpose": "any maskable"` into a single entry prevents Chrome from showing the install prompt. Always generate actual `.png` files and list them as two separate objects:
    ```json
    {"src":"icon-192.png","sizes":"192x192","type":"image/png","purpose":"any"},
    {"src":"icon-192.png","sizes":"192x192","type":"image/png","purpose":"maskable"}
    ```

## Data & export
- Export auto-includes anything nested in `getWD()` (workouts incl. bodyweight/bp), `getBD()` (baby incl. growth/medicine/milestones/teeth/bags/babySex), `getActionLog()` (incl. cardio), plus list items themselves (incl. per-item `amount`, `tags`, `today`, `recipe` fields).
- Top-level stores also exported: `food_log`, `food_notes`, `saved_meals`, `cal_goal`, `secVisible`, `syncPrefs`, `recipes` (grocery import recipes), `travel_tags`, `recipebook` (Recipes section), `mealplan` (week dinner planner, v317).
- A coverage-checklist comment sits above `exportData`. **Only touch export if a brand-new top-level `storeSet` key is added** outside the nested structures above.
- **Deliberately NOT exported/synced** (transient or device-local): `fl4_tomb_*` (deletion tombstones, 90-day purge since v295), `hearthWeather` (weather cache), `fl4_last_backup` / `fl4_backup_snooze` (backup-nudge timing), `fl4_last_cloudbackup` (daily cloud-backup stamp), `fl4_catOpen_*` / `fl4_doneOpen_*` (UI state), `fl4_seen_*` / `fl4_unseen_*` (partner-item tracking).
- **Daily cloud backup (v287):** the same `buildExportPayload()` output is PUT to `/hearth/users/<user>/backups/<YYYY-MM-DD>` once per day per device, newest 7 kept. The payload builder is shared with the file export — when adding a new top-level store, one edit covers both.

## Key storage keys
`fl4_workouts`, `fl4_baby`, `fl4_action_log`, `fl4_food_log`, `fl4_recipebook` (Recipes section — **shared household-wide** via newest-wins merge), `fl4_mealplan` (week dinner planner — **shared household-wide**, newest-wins per day, self-pruning >14d), `fl4_recipes` (grocery import recipes), `fl4_travel_tags`, `fl4_tomb_<lt>` (list deletion tombstones, transient, 90-day purge), `fl4_catOpen_*` / `fl4_doneOpen_*` (UI state, NOT exported), `fl4_seen_*` / `fl4_unseen_*` (partner-item tracking, NOT exported), `fl4_secVisible`, `fl4_syncPrefs`, `fl4_last_backup` / `fl4_backup_snooze` (backup nudge, transient), `fl4_last_cloudbackup` (cloud-backup stamp, transient), `hearthWeather` (weather cache — current + 3-day forecast; device-local, NOT exported, NOT synced).

## Recipe unit rules (Recipes section)
- tsp and tbsp **stay** as tsp/tbsp at any serving size (do not convert to ml).
- ml stays ml.
- cups convert to **grams** for dry/weighable ingredients (via a name-matched density table) or **ml** for liquids (1 cup ≈ 240 ml).
- Counts (cloves, cans, slices, pieces, cubes, pinches) scale as whole numbers and pluralise.

## Standard per-change workflow (delegated to subagents since 05/07/2026)
1. **Run the `hearth-preflight` subagent** before any edits — every time, without exception. It fetches and confirms the base is the latest `origin/main`, creates and pushes the `backup-vNNN` branch (current version, before any edits; **retention: keep the newest 10** — deletion is blocked from remote sessions (403), so it names the ones outside the window for Cathal to delete on GitHub, repo → Branches → 🗑️), reports version state across sw.js/index.html/notes, and greps for already-built features when given keywords. Resolve any flags before editing. Never push to or delete `main`.
2. Edit incrementally — never rewrite the whole file.
3. **Run the `hearth-verifier` subagent** after edits and before committing. It grep-verifies edits landed, `node --check`s the extracted main script block (PREVIEW MODE → first `</script>` AFTER it), checks file length / DOCTYPE count (2: one real + one in the print-doc string) / single version label / clean EOF, and version-bump consistency. A DO NOT COMMIT verdict means stop and fix. (If subagents are unavailable, the full manual checklists live in `.claude/agents/hearth-preflight.md` and `.claude/agents/hearth-verifier.md` — run them by hand.)
4. Bump the version string in BOTH `index.html` and `sw.js`. Format: **`vNNN · DD/MM/YYYY`** (date only — no `HH:MM`). The displayed bottom-of-page label is sourced from the `sw.js` `VERSION` constant at runtime (the HTML label is overwritten via the `SW_VERSION` postMessage), so `sw.js` is the source of truth for what the user sees; a stale-looking version means the new SW hasn't activated yet, not a bug.
5. **Update `HEARTH-notes.md`**: add a changelog entry (keep "Recently completed" to the newest ~10 detailed entries — move the surplus oldest one to `HEARTH-archive.md`), update the Current version line, and sync the backlog.
6. **Commit, push, and immediately open a GitHub PR** — always, without waiting to be asked. Cathal merges to `main` from the PR; never say "create a PR when you're ready". **One PR per version (02/07/2026):** Cathal merges within minutes of a PR opening, so pushing a second version to the same branch strands it on a merged PR (happened with v287 on PR #25 → rescued as #26). Fetch/rebase onto latest `main` before starting each version; every version gets a fresh PR. The `/release` command (`.claude/commands/release.md`) automates steps 4–6 plus the verifier run. A GitHub Action (`.github/workflows/hearth-pr-checks.yml`) re-runs the mechanical checks on every PR as a backstop.

## Device/workflow notes
- **Active workflow:** Claude Code commits + pushes to a feature branch; Cathal reviews the PR diff on GitHub and merges to `main`. GitHub Pages serves the updated app within seconds of merge.
- Backup branches (`backup-vNNN`) are the per-release restore points — frozen snapshots pushed to GitHub before every change.
- GitHub edits on mobile (if needed without Claude Code): use **Chrome with "Desktop site"** — the GitHub app can't edit files.
- The old "download zip → extract on Redmi → re-upload to GitHub" flow is superseded by the git/PR workflow. It remains a valid manual fallback if Claude Code is unavailable.

## Review council (on request)
When Cathal says "use my council" / "council review", invoke the **`hearth-council` skill** (`.claude/skills/hearth-council/SKILL.md`) — the full 7-expert spec lives there and loads only when invoked.

## Sync troubleshooting
- **A device receives but can't send.** Most likely cause is NOT a code bug. On Petra's tablet (during v255) the root cause was **low device storage evicting the saved login token / household code** from localStorage — so the device could read the shared doc but had no credentials to push. The tell is a network/auth error on login or a silent failure to send. **Fix: free up storage, then re-login** to restore the token + household code. (If it were code, the v243 `try/finally` guard would have surfaced "applyHousehold error (sync continues)" in the console — check there first to rule code in or out.)
- **Changes apply slowly / only after navigating away.** Was the pre-v246 grace-window symptom; removed in v246. If it recurs, do NOT reintroduce a blanket SSE grace window (see lesson #11) — the correct model is merge-then-re-push-if-fuller.


---

## Cross-app lessons (from My Health session, June 2026)

Lessons from a parallel vanilla React PWA session that apply equally to Hearth:

14. **`<\/script>` with a literal backslash causes a blank page with no JS errors.** Chrome's HTML parser looks for `</script` (case-insensitive) to close a script element. `<\/script>` — with a backslash — does NOT match, so external `<script src="…">` tags appear unclosed and the main inline script block never executes. The page renders blank and the console shows nothing from the app (only extension noise). This creeps in from editors or copy-paste that escape closing tags for use inside JS strings. Fix: `sed -i 's/<\\\/script>/<\/script>/g'` or a plain find/replace in the GitHub editor.

15. **Edge Tracking Prevention blocks unpkg.com/CDN scripts on `file://` URLs.** Microsoft Edge treats external CDN requests (React, etc.) as third-party trackers when the page is served from `file://`. Result: React never loads, page is blank. The tell is `"Tracking Prevention blocked access to storage for <URL>"` in the console — NOT a JS error. Fix: use Chrome for local PWA development, or serve via `npx serve .` on localhost.

16. **Browser extension errors in the console are not app errors.** The Apollo Client DevTools extension logs `content.js: ApolloClient connectToDevTools` on every page load regardless of what the page does. It is completely benign. In general, if an error references `content.js` and a line in the hundreds/thousands, it is an extension content script, not app code.

17. **Image-to-named-item mapping must always be user-verified.** Never guess photo→pose (or photo→anything) mappings from album order, filenames, or sequence. Always ask the user to open each URL and read the label/content directly. Wrong mappings are completely invisible at code time and only show up visually in the app.

18. **bash tools can go unavailable mid-session.** When `bash_tool` and `str_replace` become temporarily unavailable, present pending changes as clearly-labelled code artifacts with exact find/replace strings so the user can apply them manually or carry them to the next session. Never silently drop changes that were discussed but couldn't be applied.

19. **Per-leg/per-side timer pattern.** For timed physio/exercise exercises requiring separate timers per leg or side: add `sets:2, perLeg:true` to the exercise data object; in the UI, check `ex.perLeg` to show `"Right leg"` / `"Left leg"` instead of `"Set 1 of 2"` / `"Set 2 of 2"`. All other exercises with `perLeg` absent fall back to the normal set label automatically.

---
---

## sw.js
`sw.js` is in the repo root alongside `index.html`. Read it directly — no snapshot needed here.

It is a **single-source-of-truth file**: changing the one `VERSION` constant updates the cache name and the SW version message together, so a routine version bump is a **one-line edit**. The version format is `vNNN · DD/MM/YYYY`.
